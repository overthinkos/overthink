# Changelog — 2026-07

### 2026-07-01 — feat(charly): externalize the 4 detection-builders' BUILD-TIME multi-stage → OpResolve through their plugins (C10)

The build-time multi-stage of the four DETECTION-builders (pixi / npm / aur / cargo) moves out of the embedded
`builder:` vocabulary (`charly/charly.yml`'s `stage_template` / `copy_artifact` / `copy_binary` / cargo
`install_template`) into their existing out-of-process plugins (`candy/plugin-builder-{pixi,npm,aur,cargo}`),
resolved by an `OpResolve` build leg — the SAME seam the `external_builder:`-selected out-of-tree builders
already used. The plugins previously served ONLY the deploy-time `OpCollectContext`/`OpReverse` legs and
explicitly rejected `OpResolve`; now each dispatches `OpResolve` → the shared `kit.BuilderResolve`.

- **New render home (R3):** `charly/plugin/kit/builder_resolve.go` — the SINGLE shared render for all four
  detection-builders, holding the VERBATIM former vocabulary `stage_template` / cargo `install_template` strings
  (the only change: the two cache-mount template FUNC calls became pre-rendered input fields, so the emitted
  bytes stay byte-identical). `BuilderResolve(word, spec.BuilderResolveInput) → spec.BuilderResolveReply`.
- **New wire (`charly/spec/deploy_wire.go`):** `BuilderResolveInput` (the host-computed render context — builder
  ref, stage name, copy src, uid/gid/home, filesystem-detected manifest/lockfile/build-script, aur
  packages/options, PRE-RENDERED cache-mount flag strings) as `OpResolve` `op.Params`; `BuilderResolveReply`
  gains `CopyBinary` (once-per-builder binary copy, deduped) + `InlineFragment` (cargo's in-candy `RUN`).
- **Host (`charly/generate.go`):** `emitBuilderStages` now routes an externalized detection builder through
  `resolveDetectionBuilder` → the shared `resolveBuilderStage` (`Invoke(OpResolve)`) — connecting the plugin
  on-demand via the deploy pre-pass's `ensureBuildersConnected` (R3) — and caches the reply per (candy, builder);
  `emitBuilderArtifacts` splices the cached `CopyArtifacts` + `CopyBinary`; the inline (cargo) path in
  `writeCandySteps` splices `InlineFragment`. `resolveExternalBuilder` (the `external_builder:` path) is
  refactored to share `resolveBuilderStage` — ONE OpResolve path, not two. `writeCandySteps` now returns an
  error so a builder OpResolve failure surfaces loudly (R4).
- **Pod-overlay parity:** `stepEmitBuilder` (C1.3, the pod-overlay build-emit) renders the externalized builders
  through the SAME `kit.BuilderResolve` (in-proc) — box-build and pod-overlay share ONE render source.
- **R5 hard cutover:** the four builders' `stage_template` / `copy_artifact` / `copy_binary` / cargo
  `install_template` are DELETED from `charly/charly.yml` (the `builder:` section retains DETECTION +
  `cache_mount` + the deploy `phase.install.host` + pixi's `runtime_env`/`path_contribution`/`install_command`/
  `manylinux_fix`/`build_script` context inputs the host still reads). The frozen `charly/testdata/embedded_legacy.yml`
  (migration data-equivalence fixture) and the `candy/plugin-vm/build_defaults.yml` copy are swept identically.
  The 4 plugin candies' `version:` + `Describe` calver bump to `2026.182.0X00` and their descriptions/comments
  are corrected (the "build-time multi-stage stays the CORE embedded vocabulary" claim swept across every source
  comment, spec/kit/sdk doc, and plugin manifest — R1 claim-keyed).
- **Verification:** `go build`/`go vet`/`go test ./...` green; the four builder plugins + `plugin-vm` build;
  `box validate` on box/fedora + box/cachyos clean; box/fedora (pixi/npm) + box/cachyos (aur) regenerate
  BYTE-IDENTICAL to the pre-cutover baseline (CalVer-normalized); a real `box build fedora-test` builds the
  OpResolve-rendered `testapi-pixi-build` stage (`pixi install --frozen`), COPYs its artifacts into the final
  image, and commits successfully (R8).

### 2026-07-01 — feat(charly): externalize the Reboot InstallStep BUILD-emit → candy/plugin-installstep (C1.6 — the LAST, COMPLETING the 13-step externalization)

The BUILD-emit (pod-overlay `OpEmit`) of the `Reboot` InstallStep kind — the FIFTH and LAST host-coupled step
kind — moves out of charly's core (`charly/plugin_step_reboot.go`, the in-proc `rebootStepProvider`, now
DELETED) into the compiled-in `class:step` plugin `candy/plugin-installstep`. Reboot is SPECIAL: its build-emit
is a NO-OP — a reboot has no meaning in an image build — so UNLIKE the HOST-COUPLED C1.2–C1.5 kinds it needs
NO `stepEmitter` and NO `HostBuild` callback. It is a NO-OP-emit kind exactly like `ApkInstall` (C1.1): it
declares `Emits=false`, so `spliceClassStepEmit` skips its `OpEmit` entirely (identical to the old
`rebootStepProvider.EmitOCI` returning nil). Its DEPLOY leg is UNCHANGED — `RebootStep` still emits from the
compiler when a layer sets `reboot: true`, and the external `vm` deploy's `candy/plugin-deploy-vm` walk still
drives it host-side over `RunHostStep` → `rebootVenueAndWait` (boot_id poll + rebootable-VM-venue gate); OCI/
pod/k8s skip; the external `local:` deploy skips + warns. With C1.6 landed, EVERY builtin InstallStep kind is
now plugin-served: the 12 compiler-emitted kinds via `candy/plugin-installstep`, and `ExternalPlugin` (the
13th) via its own per-verb `class:step` plugin dispatch — leaving `ExternalPlugin` the ONLY kind with an
in-proc `StepProvider.EmitOCI`.

`reboot` is a compiler-emitted step word (from the candy `reboot: true` field), never authored as
`run: plugin: reboot`, and is NOT a verb word (no collision), so the C1.1 verb-first precedence is unaffected.

- **`candy/plugin-installstep`** — now also serves `step:reboot` (Describe advertises it with `Emits=false`;
  new `wordReboot` const + a no-op `renderFragment` case returning an empty fragment, mirroring `apk-install`).
  It is a PURE word (no `hostCoupledStepWords` entry — no `HostBuild` callback). Version 2026.182.1545 →
  2026.182.1600 (both the candy `version:` and the plugin `calver`, re-aligned — C1.5 had bumped only `calver`).
- **`OCITarget` routing** — `pluginEmitStepWords` (provider_step.go) gains `StepKindReboot → "reboot"`;
  `emitStep` routes it through the shared `spliceClassStepEmit`, which — seeing `Emits=false` — skips the
  `OpEmit` (a no-op build-emit, the exact former behaviour).
- **DELETED (hard cutover, R5)** — `charly/plugin_step_reboot.go` (the in-proc `rebootStepProvider` +
  its `registerDedicatedBuiltin` self-registration). `git grep rebootStepProvider` / `plugin_step_reboot`
  returns only this CHANGELOG. The stale `step_builtins.go` / `registry_bootstrap.go` comments naming the
  deleted file are updated to name `plugin_step_external.go` (the ONE remaining in-proc dedicated step
  provider).
- **Manifest / doc divergence swept (R1/R5, claim-keyed)** — the C1.5 (Op) cutover added `op` to the plugin's
  Describe + `pluginEmitStepWords` but omitted it from the `candy/plugin-installstep/charly.yml` `providers:`
  list, the `schema/installstep.cue` served-kinds comment, and the candy `description:` (RCA: commit
  `113e04c8` never touched `charly.yml`). Those surfaces — which C1.1–C1.4 each kept complete — are the exact
  lists this cutover edits for `reboot`, so both `step:op` (restoring the C1.5 omission) and `step:reboot` are
  added to the manifest + comment + description together. Separately, `registry_bootstrap.go`'s ClassStep
  comment was stale from C1.4/C1.5 (still listed `Op`/`LocalPkgInstall` as in-proc dedicated providers and
  omitted them from the plugin-served list) — corrected to the accurate post-C1.6 split.
- **Tests** — `TestDedicatedProviders_BulkStepResolveAndAbsent` now routes `Reboot` through the
  `pluginEmitStepWords` branch (asserts NO in-proc `StepProvider` + a compiled-in `class:step` provider with a
  `StepContract`) and stays green; its comments updated to the 12-plugin-served / 1-in-proc split. The obsolete
  `TestDedicatedProviders_ResolveAndDispatch` (whose ONLY subject was the now-plugin-served `step:Reboot`) is
  DELETED — the bulk test comprehensively covers the remaining dedicated `ExternalPlugin` provider. The
  build-emit no-op is registration-only; the deploy leg (`rebootVenueAndWait`) is untouched and covered by the
  existing `vm_deploy_lifecycle_test.go` RunHostStep RebootStep tests (`TestRebootStepViaRunHostStep_*`) +
  `vfio_test.go` (the `reboot: true` → trailing `RebootStep` compile). No light reboot bed exists (only a heavy
  VM reboot bed would re-prove the unchanged deploy leg), so this cutover is build-emit-registration-only.

### 2026-07-01 — feat(charly): externalize the Op InstallStep BUILD-emit via the step-emit HostBuild seam (C1.5)

The BUILD-emit (pod-overlay `OpEmit`) of the `Op` (task) InstallStep kind — the FOURTH and RICHEST of the
HOST-COUPLED step kinds — moves out of charly's core (`OCITarget.emitOp`, now DELETED) into the compiled-in
`class:step` plugin `candy/plugin-installstep`, riding the C1.0 `step-emit` HostBuild seam (mirrors C1.2
SystemPackages + C1.3 Builder + C1.4 LocalPkgInstall). Op's build-emit is HOST-COUPLED: it drives
`Generator.emitTasks`, the full per-verb render pipeline (COPY from the layer scratch stage, content-addressed
inline-content staging, adjacent mkdir/link/setcap coalescing, and the act-verb `case "plugin"` seam the box
build shares) — needing the scanned Candy set (`Generator.candyByName`), the box UID/GID/Home (`ResolvedBox`),
and the per-image build dir + build-context prefix — none of which can cross the process boundary. So the
plugin's `OpEmit` for `op` calls back the host's `step-emit` host-builder (`Executor.HostBuild`) and echoes the
returned fragment; the render stays in core, UNCHANGED (R3 — the SAME `Generator.emitTasks` the box build
`generate.go writeCandySteps` already calls, so the pod-overlay and box-build op-emit paths can never drift).

**`compileActOp` is UNCHANGED (the C1.1 verb-first fix intact).** This cutover moves ONLY the OpStep's
BUILD-emit; the compile is untouched — a `run: plugin: <verb>` still lowers to a native `OpStep` (verb-first
precedence: an in-proc `ProvisionActor`/`command` verb wins over a colliding `step:` word), and its DEPLOY leg
still renders via `resolveProvisionScript` / `renderOpCommand`. `op` joins the compiler-emitted step-word set
(never authored as `run: plugin: op`), so it cannot regress the verb-first path
(`TestCompileActOp_VerbWordWinsOverCollidingStepWord` re-run green).

- **`charly/step_emit_hostbuild.go`** — registers the FOURTH `stepEmitter`: `stepEmitOp`
  (`registerStepEmitter("op", …)`). It reconstructs the concrete `*OpStep` from the wire view (`stepFromView`),
  resolves the candy by its bare name (`build.Generator.candyByName` — nil-safe, with the remote qualified-key
  add_candy fallback), and drives the SAME `Generator.emitTasks` (reading `build.Box` + `build.ImageBuildDir` +
  `build.ContextRelPrefix`) the former in-proc render used — byte-equivalent; a synthetic path without a
  Generator/Box yields the SAME informative comment; a candy the scan never saw is a LOUD error.
- **`charly/builder_venue.go`** — `buildEngineContext` gains a `ContextRelPrefix` field (OCITarget's
  build-context-relative prefix for content-addressed inline-content staging), populated ONLY by the pod-overlay
  build-emit path (`OCITarget.stepEmitBuildContext`, now threading it alongside the C1.2 `DistroCfg`, C1.3
  `Generator`/`BuilderConfig`/`Box`, and C1.4 `ImageBuildDir`); zero on every deploy-leg context.
- **`candy/plugin-installstep`** — now also serves `step:op` (Describe advertises it with `Emits=true`; added
  to the `hostCoupledStepWords` set + `wordOp` const). Its `Invoke(OpEmit)` routes `system-packages` (C1.2),
  `builder` (C1.3), `local-pkg-install` (C1.4), and `op` (C1.5) through the HOST-COUPLED `emitViaHostBuild`
  branch; the seven PURE kinds keep their direct-format path. Version 2026.182.1430 → 2026.182.1545.
- **`OCITarget` routing** — `pluginEmitStepWords` (provider_step.go) gains `StepKindOp → "op"`; `emitStep`
  routes it through the shared `spliceClassStepEmit`, whose in-proc reverse channel now carries
  `ContextRelPrefix` in the box BUILD-ENGINE context (`stepEmitBuildContext`).
- **DELETED (hard cutover, R5)** — `charly/plugin_step_op.go` (the in-proc `opStepProvider`), the `emitOp`
  method on `OCITarget`, and its now-orphaned `lookupCandy` helper (a thin nil-guarded wrapper over the
  nil-safe `Generator.candyByName`, its last caller gone) + its dedicated test (retargeted to `candyByName`).
- **Tests** — `TestOCITargetEmitOpViaPlugin` (new): drives the FULL chain `OpStep → OCITarget.Emit → emitStep
  → pluginEmitStepWords[Op]="op" → spliceClassStepEmit → plugin OpEmit → emitViaHostBuild → HostBuild("step-emit")
  → stepEmitOp → emitTasks`, asserting both a RUN (mkdir) and a COPY (layer scratch stage); FAILS without the
  routing (proven — the bijection gate panics `unserved InstallStep kinds: [Op]`). `TestEmitOp_PluginAct_UnixGroup_OCI`
  rewritten to drive the same chain via `Emit`.

### 2026-07-01 — feat(charly): externalize the LocalPkgInstall InstallStep BUILD-emit via the step-emit HostBuild seam (C1.4)

The BUILD-emit (pod-overlay `OpEmit`) of the `LocalPkgInstall` InstallStep kind — the THIRD of the
HOST-COUPLED step kinds — moves out of charly's core (`OCITarget.emitLocalPkgInstall`, now DELETED) into the
compiled-in `class:step` plugin `candy/plugin-installstep`, riding the C1.0 `step-emit` HostBuild seam (mirrors
C1.2 SystemPackages + C1.3 Builder). LocalPkgInstall's build-emit is HOST-COUPLED: its render
(`renderLocalPkgImageInstall`) reads the box-type switch off the `Generator` (`DevLocalPkg`) and, for a
disposable check bed, BUILDS the candy's package from LOCAL in-development source ON THE HOST
(`buildLocalPkgOnHost` — makepkg / a distro-matched podman container) and STAGES the built file into the
per-image build dir — none of which can cross the process boundary. So the plugin's `OpEmit` for
`local-pkg-install` calls back the host's `step-emit` host-builder (`Executor.HostBuild`) and echoes the
returned fragment; the render stays in core, UNCHANGED (R3 — the SAME `renderLocalPkgImageInstall` the image
build `generate.go writeCandySteps` already calls, so the two image-build paths can never drift). The DEPLOY
leg is UNCHANGED — `LocalPkgInstall` still compiles natively (`compileLocalPkgStep`, never `external:<word>`)
and the deploy walk routes it host-side via `RunHostStep` → `execLocalPkgInstall` (`localpkg.go`: makepkg +
`pacman -U` / `dnf install` / `apt-get install`), already exercised by the `check-exampledeploy` bed.

- **`charly/step_emit_hostbuild.go`** — registers the THIRD `stepEmitter`: `stepEmitLocalPkgInstall`
  (`registerStepEmitter("local-pkg-install", …)`). It reconstructs the concrete `*LocalPkgInstallStep` from
  the wire view (`stepFromView`), then calls the SAME `renderLocalPkgImageInstall` (dev switch off
  `build.Generator.DevLocalPkg`, `boxName` off `build.Box.Name`, `imageDir` off `build.ImageBuildDir`) the
  former in-proc render used — byte-equivalent (production release-download RUN; dev in-development build +
  `COPY`; a nil `LocalPkg` renders nothing).
- **`charly/builder_venue.go`** — `buildEngineContext` gains an `ImageBuildDir` field (OCITarget's per-image
  overlay build dir — distinct from `Generator.BuildDir`), populated ONLY by the pod-overlay build-emit path
  (`OCITarget.stepEmitBuildContext`, now threading it alongside the C1.2 `DistroCfg` + C1.3
  `Generator`/`BuilderConfig`/`Box`); zero on every deploy-leg context (the LocalPkgInstall deploy leg
  `execLocalPkgInstall` reads none of them).
- **`candy/plugin-installstep`** — now also serves `step:local-pkg-install` (Describe advertises it with
  `Emits=true`; added to the `hostCoupledStepWords` set + the manifest `providers:`). Its `Invoke(OpEmit)`
  routes `system-packages` (C1.2), `builder` (C1.3), and `local-pkg-install` (C1.4) through the HOST-COUPLED
  `emitViaHostBuild` branch; the seven PURE kinds keep their direct-format path. Version 2026.182.1300 →
  2026.182.1430.
- **`OCITarget` routing** — `pluginEmitStepWords` (provider_step.go) gains `StepKindLocalPkgInstall →
  "local-pkg-install"`; `emitStep` routes it through the shared `spliceClassStepEmit`, whose in-proc reverse
  channel already carries the box BUILD-ENGINE context (`stepEmitBuildContext`, extended with `ImageBuildDir`).
- **DELETED (hard cutover, R5)** — `charly/plugin_step_local_pkg_install.go` (the in-proc
  `localPkgInstallStepProvider`) and the `emitLocalPkgInstall` method on `OCITarget`.
  `checkStepProviderBijection` now serves `LocalPkgInstall` via the plugin route (no in-proc `StepProvider`),
  like SystemPackages + Builder + the seven PURE kinds. `step_builtins.go` comment swept.
- **NO word collision** — `step:local-pkg-install` is a fresh registry key `(ClassStep,"local-pkg-install")`.
  `localpkg` is a candy authoring `DataKey` (the `localpkg:` field), NOT a verb word, and it is a DIFFERENT
  string; there is NO `verb:local-pkg-install`, and `LocalPkgInstallStep` is compiled from the candy's
  `localpkg:` source (`compileLocalPkgStep`), never `compileActOp` — so the C1.1 verb-first precedence does
  not apply (identical reasoning to C1.2/C1.3).
- **Stale-claim sweep (R1/R5, in-scope)** — the `install_plan.go` `LocalPkgInstallStep` doc claimed "OCITarget
  SKIPS it — no makepkg in a container image build", which was FALSE (the image build + pod-overlay build-emit
  both RENDER via `renderLocalPkgImageInstall`); reworded to the accurate download-vs-build behaviour. The
  misnamed `TestOCITargetSkipsLocalPkg` (asserted "OCITarget skips localpkg" but only passed because its step
  carried a nil `LocalPkg`) is renamed `TestOCITargetLocalPkgNilContractEmitsNothing` and reworded to the real
  invariant (a nil-`LocalPkg` step renders nothing through the plugin chain).
- **R10 coverage** — new real-chain e2e test `TestOCITargetEmitLocalPkgInstallViaPlugin`
  (`build_target_oci_test.go`) drives the FULL chain (`LocalPkgInstallStep` → `OCITarget.Emit` → `emitStep` →
  `pluginEmitStepWords` → `spliceClassStepEmit` → the compiled-in plugin `OpEmit` → `emitViaHostBuild` →
  `HostBuild("step-emit")` → `stepEmitLocalPkgInstall`) against the in-proc HostBuild, asserting the
  byte-equivalent production release-download + `pacman -U` output (FAILS without this change — the bijection
  gate panics `unserved InstallStep kinds: [LocalPkgInstall]`). The DEPLOY leg (`execLocalPkgInstall`: makepkg
  + `pacman -U` → `charly-f3-witness`) is unchanged and stays covered by the `check-exampledeploy` bed
  (`examplebuild-localpkg`); no new bed needed.

### 2026-07-01 — feat(charly): externalize the Builder InstallStep BUILD-emit via the step-emit HostBuild seam (C1.3)

The BUILD-emit (pod-overlay `OpEmit`) of the `Builder` InstallStep kind — the SECOND of the HOST-COUPLED step
kinds — moves out of charly's core (`OCITarget.emitBuilder`, now DELETED) into the compiled-in `class:step`
plugin `candy/plugin-installstep`, riding the C1.0 `step-emit` HostBuild seam (mirrors C1.2 SystemPackages).
Builder's build-emit is HOST-COUPLED and HIGHER-RISK than SystemPackages: its render needs the multi-stage
builder ENGINE (the `BuilderConfig` registry, `Generator.buildStageContext`, the box `ResolvedBox` UID/GID +
`builder:` image ref, `RenderTemplate`), none of which can cross the process boundary. So the plugin's
`OpEmit` for `builder` calls back the host's `step-emit` host-builder (`Executor.HostBuild`) and echoes the
returned fragment; the render stays in core, UNCHANGED (R3 — the SAME `buildStageContext` + `RenderTemplate`
pipeline, inline + multi-stage branches). The DEPLOY leg is UNCHANGED — `BuilderStep` still compiles natively
(`compileBuilderSteps`, never `external:<word>`) and the deploy walk routes it host-side via `RunHostStep` →
`runVenueBuilderStep` (`builder_venue.go`).

- **`charly/step_emit_hostbuild.go`** — registers the SECOND `stepEmitter`: `stepEmitBuilder`
  (`registerStepEmitter("builder", …)`). It reconstructs the concrete `*BuilderStep` from the wire view
  (`stepFromView`), then reuses the SAME inline/multi-stage render the former `OCITarget.emitBuilder` ran
  (`Generator.buildStageContext` + `RenderTemplate`), reading the threaded host build engine off the
  reverse-channel `buildEngineContext`. Byte-equivalent to the former in-proc render (inline `USER <uid>` +
  `install_template`; multi-stage `FROM <builder> AS <stage>` from `stage_template`); a nil BuilderConfig /
  Box / layer yields the SAME informative skip comment, an undefined builder or a template error is a LOUD
  failure (R4).
- **`charly/builder_venue.go`** — `buildEngineContext` gains `Generator` / `BuilderConfig` / `Box` fields,
  populated ONLY by the pod-overlay build-emit path (`OCITarget.stepEmitBuildContext`, now threading them
  alongside the C1.2 `DistroCfg`); zero on every deploy-leg context (the Builder deploy leg reads none of
  them).
- **`candy/plugin-installstep`** — now also serves `step:builder` (Describe advertises it with `Emits=true`).
  Its `Invoke(OpEmit)` routes both `system-packages` (C1.2) and `builder` (C1.3) through the HOST-COUPLED
  `emitViaHostBuild` branch (a `hostCoupledStepWords` set); the seven PURE kinds keep their direct-format
  path. Version 2026.182.1200 → 2026.182.1300.
- **`OCITarget` routing** — `pluginEmitStepWords` (provider_step.go) gains `StepKindBuilder → "builder"`;
  `emitStep` routes it through the shared `spliceClassStepEmit`, whose in-proc reverse channel already carries
  the box BUILD-ENGINE context (`stepEmitBuildContext`, extended with the builder engine).
- **DELETED (hard cutover, R5)** — `charly/plugin_step_builder.go` (the in-proc `builderStepProvider`) and the
  `emitBuilder` method on `OCITarget`. `checkStepProviderBijection` now serves `Builder` via the plugin route
  (no in-proc `StepProvider`), like SystemPackages + the seven PURE kinds. `step_builtins.go` /
  `registry_bootstrap.go` comments swept.
- **NO word collision** — `step:builder` is a fresh registry key `(ClassStep,"builder")`, distinct from the
  pre-existing `kind:builder` (`candy/plugin-builder`, the `builder:` entity kind) and `builder:{aur,cargo,npm,pixi}`
  (the detection builders, `ClassBuilder`); the registry keys on `(class, word)`. There is NO `verb:builder`,
  and `BuilderStep` is compiled from builder DETECTION (`compileBuilderSteps`), never `compileActOp` — so the
  C1.1 verb-first precedence does not apply (identical reasoning to C1.2's `system-packages`).
- **Builder-image threading fix (RDD-surfaced by the new bed, R2 in-scope)** — the new `check-builder-local`
  bed surfaced a PRE-EXISTING gap (unrelated to the build-emit externalization): an OUT-OF-PROCESS
  `kind:local`/`vm` deploy's `install_opts.builder_image` never reached the builder-step image resolution.
  It merged only into `EmitOpts.BuilderImageOverride` (consumed at APPLY by `builderStepImage`,
  `builder_venue.go`), which does NOT cross into the out-of-process deploy walk; and at COMPILE
  `hostCtx.BuilderImage` was empty, so `resolveBuilderImage` left `BuilderStep.BuilderImage` empty — the
  out-of-process walk's `builderStepImage` then failed `no builder image for <builder>`. This path was never
  exercised before (no bed deployed a detection-builder candy to a local:/vm: venue). FIX (a — the image
  travels IN the compiled step): `bundle_add_cmd.go` captures the deploy's effective override
  (`opts.BuilderImageOverride`, CLI-over-`install_opts` precedence already merged) into a per-node
  `builderImageOverride` field (mirroring `vmEntity`) before `compileNodePlans`, and a new
  `compileHostContext()` helper seeds `hostCtx.BuilderImage` from it — so `resolveBuilderImage` (unchanged,
  `hostCtx.BuilderImage` > `img.Builder`) sets `BuilderStep.BuilderImage`, which round-trips through the step
  view (`step_view.go`) to the out-of-process walk. The namespaced ref (`fedora.fedora-builder`) resolves to a
  concrete image later at `BuilderRun` → `EnsureImagePresent` (unchanged, R3). Empty override → the unchanged
  path. Proven: `bundle add check-builder-local --dry-run --format json` now compiles the npm BuilderStep with
  `BuilderImage: "fedora.fedora-builder"` (was empty).
- **R10 coverage** — new real-chain e2e tests `TestOCITargetEmitBuilderInlineViaPlugin` +
  `TestOCITargetEmitBuilderMultiStageViaPlugin` (`build_target_oci_test.go`) drive the FULL chain
  (`BuilderStep` → `OCITarget.Emit` → `emitStep` → `pluginEmitStepWords` → `spliceClassStepEmit` → the
  compiled-in plugin `OpEmit` → `emitViaHostBuild` → `HostBuild("step-emit")` → `stepEmitBuilder`) against the
  in-proc HostBuild, asserting the byte-equivalent inline `cargo install` + multi-stage `FROM … AS …` output
  (both FAIL without this change). NEW builder-DEPLOY-leg bed `check-builder-local` (a `kind:local` host
  deploy composing the new minimal npm builder fixture `candy/check-builder-npm`, builder image pinned via
  `install_opts.builder_image: fedora.fedora-builder`) closes the flagged gap — before it, no disposable bed
  deployed a pixi/npm/cargo/aur builder candy to a local:/vm: venue, so `runVenueBuilderStep` was only
  transitively exercised.

### 2026-07-01 — feat(charly): externalize the SystemPackages InstallStep BUILD-emit via the step-emit HostBuild seam (C1.2)

The BUILD-emit (pod-overlay `OpEmit`) of the `SystemPackages` InstallStep kind — the FIRST of the five
HOST-COUPLED step kinds — moves out of charly's core (`OCITarget.emitSystemPackages`, now DELETED) into the
compiled-in `class:step` plugin `candy/plugin-installstep`, riding the C1.0 `step-emit` HostBuild seam. UNLIKE
the seven PURE kinds (C1.1), SystemPackages' build-emit is HOST-COUPLED: its render needs the host build
ENGINE (the `DistroDef` format templates + `formatPhaseTemplate` + `RenderTemplate`), which cannot cross the
process boundary. So the plugin's `OpEmit` for `system-packages` calls back the host's `step-emit`
host-builder (`Executor.HostBuild`) and echoes the returned fragment; the render stays in core. The DEPLOY
leg is UNCHANGED — `SystemPackagesStep` still compiles natively (`compileSystemPackageSteps`, never
`external:<word>`) and the deploy walk routes it host-side via `RunHostStep` → `renderHostPackageCommand`.

- **`charly/step_emit_hostbuild.go`** — registers the FIRST `stepEmitter`: `stepEmitSystemPackages`
  (`registerStepEmitter("system-packages", …)`). It reconstructs the concrete step from the wire view
  (`stepFromView`), resolves the box-specific `FormatDef` via `DistroConfig.FindFormat` (the SAME resolver the
  host deploy render `renderHostPackageCommand` uses — R3), and renders the format's `phase.install.container`
  template. The render is byte-equivalent to the former `OCITarget.emitSystemPackages`.
- **`candy/plugin-installstep`** — now also serves `step:system-packages` (Describe advertises it with
  `Emits=true`). Its `Invoke(OpEmit)` gained a HOST-COUPLED branch (`emitViaHostBuild`): it reaches the host
  executor (`sdk.ExecutorForInvoke` — the in-proc context executor when compiled-in), wraps the step VIEW
  (`op.Params`) + `BuildEnv.Distros` (`op.Env`) into a `spec.StepEmitRequest`, calls
  `Executor.HostBuild("step-emit", …)`, and echoes the returned `spec.EmitReply`. The seven PURE kinds keep
  their direct-format path. Version 2026.182.1000 → 2026.182.1200.
- **`OCITarget` routing** — `pluginEmitStepWords` (provider_step.go) gains
  `StepKindSystemPackages → "system-packages"`; `emitStep` routes it through the shared
  `spliceClassStepEmit`, whose in-proc reverse channel is now threaded with the box BUILD-ENGINE context
  (`stepEmitBuildContext` → `wrapDistroDef(t.DistroDef)`, a new `format_config.go` helper) so the host-coupled
  step can resolve its `FormatDef` in the `step-emit` host-builder.
- **DELETED (hard cutover, R5)** — `charly/plugin_step_system_packages.go` (the in-proc
  `systemPackagesStepProvider` + its `OCITarget.emitSystemPackages` call) and the `emitSystemPackages` method
  on `OCITarget`. `checkStepProviderBijection` now serves `SystemPackages` via the plugin route (no in-proc
  `StepProvider`), like the seven PURE kinds. `step_builtins.go` comments swept.

### 2026-07-01 — feat(charly): externalize the 7 PURE builtin InstallStep kinds' BUILD-emit to candy/plugin-installstep (C1.1)

Riding the C1.0 F-STEP-EMIT foundation, the BUILD-emit (pod-overlay OpEmit) of the seven PURE builtin
InstallStep kinds — `File`, `ShellHook`, `ShellSnippet`, `ServicePackaged`, `ServiceCustom`, `RepoChange`,
`ApkInstall` — moves out of charly's core (the `OCITarget.emit*` methods) into a compiled-in `class:step`
plugin. RDD-classified as PURE: each build-emit is pure string formatting from the compiler-produced
`spec.InstallStepView` (no DistroDef / Generator / emitTasks), so the plugin returns the Containerfile
fragment directly from `OpEmit` — no host build engine, no `step-emit` HostBuild round-trip. The DEPLOY leg
for these kinds is UNCHANGED: `charly/plugin/kit.WalkPlans` (`walkFile`/`walkShellHook`/…) still renders them
from the SAME view over the executor reverse channel. This is build-emit-only externalization; full
externalization (compiler → `external:<word>`, deploy via `OpExecute`) is a later M2 cutover.

- **`candy/plugin-installstep`** — a new compiled-in DUAL-PLACEMENT `class:step` plugin (its own Go module,
  `NewProvider`/`NewMeta` + a `cmd/serve` out-of-process shim, listed in `charly/charly.yml`
  `compiled_plugins:`). Its `Describe` advertises seven `step:<word>` capabilities each with a
  `StepContract{Emits}` — `Emits=true` for the six that bake a fragment, `Emits=false` for `apk-install`
  (no device at image build). Its `Invoke(OpEmit)` decodes `spec.InstallStepView` and renders the SAME
  string the former `emitFile`/`emitShellHook`/`emitShellSnippet`/`emitServicePackaged`/`emitServiceCustom`/
  `emitRepoChange` produced. No authored `plugin_input` (these kinds are compiler-emitted, never authored as
  a `plugin:` step), so no `InputDef`; the shipped `.cue` schema is vestigial (present only to satisfy the
  plugin load gate). Version 2026.182.1000.
- **`OCITarget.emitStep` routing** (build_target_oci.go): a new `pluginEmitStepWords` map (provider_step.go)
  maps each of the seven kinds to its plugin word; `emitStep` serializes the step VIEW (`stepToView`) as the
  `OpEmit` payload and splices via the new shared `spliceClassStepEmit`, which `emitExternalStep` (the F3
  authored external-step arm) now also calls (R3). `invokeOpEmitFragment` gained an `allowEmpty` variant
  (`invokeOpEmitFragmentOpt`, tasks.go): a compiler-emitted step can render legitimately-empty (an empty
  shell snippet, a packaged service with no overrides + enable=false, a custom service with no unit text —
  exactly the cases the former `emit*` returned nothing), while an authored external step declaring
  `Emits=true` still must produce a fragment.
- **DEPLOY-side fix (word collision, R1/R2 — same cutover)** — registering `step:file` made the word
  `file` collide with the pre-existing `verb:file` (candy/plugin-file, a ProvisionActor that drops a
  file — used by e.g. `check-local-layer`'s `run: plugin: file` marker). `compileActOp` then fell
  through the ClassVerb block into the class:step authored-external-step branch and lowered
  `run: plugin: file` to an `external:file` step → the deploy walk routed it to `OpExecute`, which the
  build-emit-only compiled-in plugin cannot serve (`external step "file": provider has no deploy-context
  execute`). Fix: **verb-first precedence** in `compileActOp` — a `run: plugin: <word>` that resolves as
  a VERB is a verb act (falls through to the generic `OpStep`, deploy via `resolveProvisionScript`); the
  class:step authored-external-step branch is reached ONLY when the word is NOT a verb (a genuine
  external step KIND like `examplestepkind`). The seven build-emit-only class:step words are never
  authored as `run: plugin:` ops (`file` is a verb; the other six are compiler-emitted NATIVE kinds), so
  they never hit that branch. Guarded by `TestCompileActOp_VerbWordWinsOverCollidingStepWord`;
  `TestExternalStepKind_EndToEnd` proves the else-if branch still lowers a real external step KIND.
  Confirmed by `bundle add check-local --dry-run`: `check-local-layer` compiles to `Op: 2` (was `Op: 1`
  + `external:file: 1`), `examplestep-deploy-consumer` still `ExternalPlugin: 1`.
- **HARD CUTOVER (R5)** — deleted the seven dedicated `plugin_step_{file,shell_hook,shell_snippet,
  service_packaged,service_custom,repo_change,apk_install}.go` `StepProvider`s and their six
  `OCITarget.emit*` methods (apk had no method — its provider returned nil). The typed step STRUCTS +
  `Scope()`/`Venue()`/`Reverse()` methods stay in install_plan.go (they still compile from candy fields and
  project to the deploy view). `allStepKinds` stays all 13 (each round-trips through `stepToView`/
  `stepFromView` — `step_view_test` unchanged); `checkStepProviderBijection` now asserts each kind is SERVED
  — the seven `pluginEmitStepWords` kinds by a compiled-in `class:step` plugin declaring a `StepContract`,
  every other kind by an in-proc `StepProvider`. `TestDedicatedProviders_BulkStepResolveAndAbsent` +
  `step_builtins.go` doc + the stale `OCITarget.emitShellSnippet`/`emitServicePackaged` comments in
  generate.go/layers.go/install_act_test.go swept. Grep-clean: no `*StepProvider`/`emit*` references remain
  outside `CHANGELOG/` + the new candy.
- **Determinism** — the relocated `ShellHook` render now emits `ENV` keys in sorted order (the former
  `emitShellHook` iterated the map in random order — a latent pod-overlay cache-instability); matches the
  box-build `emitVarsEnv` path.
- **Regen** — `charly/plugins_generated.go` + `go.work` regenerated by `pluginsgen`
  (`TestPluginsGenReproducible` green). `go build`/`go vet`/`go test ./...` green (incl. `TestGenReproducible`,
  the step bijection, `TestStepView_RoundTrip`, `TestOCITargetEmit*` now routing through the plugin,
  `TestServicePluginActEmitsIntoBoxBuild`, `TestBuiltinPluginSchemasSplice`); `charly box validate` clean on
  the root project + box/fedora; gofmt clean.

### 2026-07-01 — feat(charly): F-STEP-EMIT — a BUILD-TIME (OpEmit) leg for the F3 external:<word> step carrier (C1.0)

The ENABLER (foundation) C1 needs to externalize the 13 builtin step kinds. RDD confirmed: the F3
`external:<word>` step carrier (install_plan.go) already had a DEPLOY leg (`executeExternalStep` →
OpExecute over the reverse channel), but NO BUILD leg — its `EmitOCI`→Containerfile fragment (used ONLY by
the pod-overlay `OCITarget`, deploy-mode add_candy synthesis) had no external arm. Without it, a step kind
whose `EmitOCI` produces a Containerfile fragment (system-packages, builder, op, localpkg — most of the 13)
could not be relocated to a plugin. This cutover adds that BUILD leg + its witness. Foundation-only — no
step kind is relocated yet.

- **`StepContract.Emits`** (proto `StepContract.emits`, `sdk.StepContract.Emits`, internal `stepContract.Emits`):
  a class:step plugin DECLARES in its Describe whether the step produces a build-context Containerfile
  FRAGMENT. `BuildCapabilities` carries it; both `grpcProvider` (out-of-proc) and `inprocProvider`
  (compiled-in) populate it (R3 placement parity), read back via `declaredStepContract()`.
- **`OCITarget.emitStep` open external-step arm** (`emitExternalStep`, build_target_oci.go): when the step
  kind is `external:<word>` (no compiled-in `StepProvider` — its serving provider is a class:step plugin
  keyed on the trimmed WORD), the arm resolves the provider, consults its declared `Emits`: `Emits=true` →
  Invoke OpEmit and splice the fragment; `Emits=false` → skip (a deploy-only external step, like apk on an
  image build). The Invoke rides the SHARED `invokeOpEmitFragment` seam (refactored out of `emitPluginFragment`
  — R3, ONE OpEmit→EmitReply→empty-guard→fragment path for the verb AND step build-emit legs). The ctx carries
  an IN-PROC reverse channel (the SAME `sdk.ContextWithExecutor` + `executorReverseServer` `dispatchBuild`
  threads for the compiled-in build:box plugin — R3), so a HOST-COUPLED step can call back HostBuild during
  its OpEmit; a PURE step ignores it and returns the fragment directly.
- **The `step-emit` HostBuild seam** (step_emit_hostbuild.go, `registerHostBuilder("step-emit",
  hostBuildStepEmit)`): the BUILD-context sibling of the F10 `overlay`/`image`/`containerfiles` host-builders,
  for a HOST-COUPLED external step whose fragment needs the host build ENGINE (DistroDef format templates,
  the Generator's rendering — machinery a []byte can't carry). It dispatches by step WORD to a per-word
  `stepEmitters` registry (`registerStepEmitter`), returning the fragment as a `spec.EmitReply` (R3). GENERIC
  (no per-word case here); the registry is EMPTY in the foundation — C1 registers a renderer when it relocates
  a host-coupled step kind. An unregistered word is a LOUD error (never a silent empty bake, R4). New wire type
  `spec.StepEmitRequest{Word,Payload,Distros}` (SDK-importable).
- **Witness** — `candy/plugin-example-stepkind`: the reference OUT-OF-TREE class:step plugin now ALSO serves the
  BUILD leg. Its Describe declares `StepContract{…, Emits:true}`; its Invoke gains an OpEmit case returning a
  Containerfile RUN that bakes `/etc/examplestepkind-build-baked` (carrying the opaque payload marker — proving
  the Payload round-trips through OpEmit too). The existing OpExecute (deploy) leg is UNCHANGED. Aligned to the
  clean `plugin-example-step` pattern (provider + module-present ADE check; the self-demo `run:`/deploy-marker
  `check:` steps were removed — they double-baked in an overlay and referenced a /tmp marker absent in a baked
  pod; the deploy leg stays covered by `TestExternalStepKind_EndToEnd`). Version 2026.182.0900.
- **Consumer** — `candy/stepkind-build-consumer` (new): the STEP-class analogue of `examplestep-consumer`, a
  candy with a BUILD-context `run: plugin: examplestepkind` step + a runtime `check:` asserting the baked marker.
- **R10 bed** — `check-stepkind-emit-pod` (box/fedora): a disposable pod bed reusing the check-pod image,
  add_candy-overlaying plugin-example-stepkind + stepkind-build-consumer; the overlay build routes the external
  step through the new OCITarget arm, baking the marker; the bed asserts it INSIDE the running container. The
  pod-overlay STEP analogue of check-addcandy-pod / check-pod's build-context `examplestep-build-baked` (a VERB).
- Tests: `TestExternalStepKind_EndToEnd` extended to assert `Emits=true` AND to drive the OCITarget arm
  (Invoke OpEmit → spliced fragment carries the baked path + payload marker); `TestStepEmitHostBuilder` proves
  the generic step-emit HostBuild seam (registered kind, per-word dispatch, loud unregistered-word error).
- **Scope**: the ENABLER ONLY. No builtin step kind is relocated; the `stepEmitters` registry is empty. NOTE:
  requirement to "extend `kit.StepProvider` with EmitFragment" was RDD-corrected — the existing `kit.StepProvider`
  is the verb→typed-step lowering role (used by plugin-package/plugin-service); adding a method would silently
  break them, and the F5 precedent delivers foundations via wire types + Invoke, not kit method additions. The
  compiled-in build-emit capability is served, placement-invisibly, by the SAME Invoke(OpEmit) (in-proc pb
  envelope) + the HostBuild seam — no bespoke kit method needed.

R10 (pending — operator-run): `charly check run check-stepkind-emit-pod` (box/fedora) through a fresh
`charly update`. go test ./... green (incl. the extended e2e + TestStepEmitHostBuilder + TestGenReproducible +
TestPluginsGenReproducible); go build/vet clean; task cue:gen + proto:gen reproducible; `charly box validate`
clean on root + box/fedora; the example-stepkind plugin builds standalone (GOWORK=off); gofmt clean.

### 2026-07-01 — refactor(charly): route the pod-overlay build through the uniform F10 "overlay" host-builder (C14.3)

`podSubstrateLifecycle.PrepareVenue` (the host-side lifecycle hook for the `pod` deploy substrate) built the
add_candy overlay container image by constructing a `PodDeployTarget` INLINE and calling `.Emit` — the LAST
inline image-build construction not routed through the uniform F10 `hostBuilders` registry (after C14-core
landed `charly box build`/`generate` through the `image`/`containerfiles` host-builders). Now PrepareVenue
REQUESTS the overlay build through the registry (`hostBuilderFor("overlay")`), completing the
build-dispatch unification: EVERY image-build construction goes through ONE seam.

- **New `overlay` host-builder** (`registerHostBuilder("overlay", hostBuildOverlay)`, pod_deploy_lifecycle.go) —
  the pod-substrate sibling of `image`/`containerfiles` (build.go) + `plugin-binary` (plugin_dispatch_reverse.go).
  A generic ACTION noun, NOT a provider WORD (the F11 uniform-API gate `TestNoSinglePluginAPISurface` forbids
  one on this surface).
- **The build ENGINE is UNCHANGED, only relocated** — the prior inline PrepareVenue body (NewGenerator +
  ResolvedBox + base-image DistroDef + baseRef CalVer + candy-secret injection + `PodDeployTarget.Emit`
  overlay synthesis) moved VERBATIM into `runOverlayBuild`, the host-side engine behind the host-builder. It
  stays host-side, in-process — nothing crosses gRPC (exactly as vm builds its disk host-side). `PodDeployTarget`
  is now constructed ONLY there.
- **spec.OverlayBuildRequest / spec.OverlayBuildReply** (spec/deploy_wire.go, sibling to BuildRequest/BuildReply):
  the pod-overlay dispatch envelope. The request carries ONLY the serializable scalars the engine cannot
  reconstruct from Dir (DeployName/Image/Version/DryRun + the gate flags); everything heavy (Config/ResolvedBox/
  Candy/DistroDef) is reconstructed HOST-SIDE from Dir via NewGenerator, exactly as runBoxBuild does. Reply
  carries {OverlayRef, BaseImage, DeployName, Error} — a build FAILURE rides Error (the reply-error convention).
- **The overlay build's LIVE inputs ride the ctx, not the []byte payload** — the compiled InstallPlans and,
  for a nested pod-in-pod overlay, the parent venue executor (`ParentExec`) + node. A live `DeployExecutor` is
  not serializable, so `overlayBuildInputs` threads them on the ctx (the SAME mechanism `sdk.ContextWithExecutor`
  uses across the reverse channel). This is the "minimal addition" the venue-as-an-input demands; it preserves
  nested pod-in-pod overlay with ZERO loss.
- **DIRECT registry call, no plugin round-trip** — PrepareVenue is ALREADY the host-side pod lifecycle hook,
  so the engine it dispatches is host-side too; it looks up + calls the registered builder directly (like
  `ensureBuilderImageBuilt` calls the host image engine without a reverse-channel hop), NOT through a
  compiled-in plugin like `charly box build` does. The `hostBuilders` map's fixed `(ctx, specJSON, build)`
  signature is honored; the ctx carries the live inputs.
- Coverage: `TestOverlayHostBuilderRegistered` (the "overlay" kind is registered + is not a provider word) +
  `TestOverlayBuildInputsCtxRoundTrip` (the live-input carrier) added; the existing pod-overlay engine tests
  (`TestPodOverlayInlineCopyResolvesUnderContext`, `TestPodOverlayStagesRemoteCandySource`) still cover the
  unchanged `PodDeployTarget`/`OCITarget` build.

R10 (pending — operator-run): the add_candy overlay beds — check-addcandy-pod (box/fedora), check-exampledeploy
(root), check-local — through a fresh `charly update`. go test ./... green; go build/vet clean; `charly box
validate` clean; gofmt clean; the other F10 host-builders (image/containerfiles) unaffected.

### 2026-07-01 — refactor(charly): drop the redundant NewGenerator+Generate preflight in RemoteImageContext.BuildImage (C14-core follow-up)

`RemoteImageContext.BuildImage` (the `charly box build @<remote-ref>` / ensure-image remote-build path)
carried a standalone `NewGenerator + gen.Generate()` preflight BEFORE its `buildCmd.Run()`. Now that box
build dispatches through candy/plugin-build → `HostBuild("image")` → `runBoxBuild` (which itself runs
`NewGenerator + Generate + buildImages` from `ctx.CacheDir` after the chdir), that preflight was pure
redundant work whose `.build/` output `runBoxBuild` immediately regenerates (a superset — box-scoped +
tagged, vs the preflight's empty opts). Deleted (R3/R2 immediate-next surfaced by C14-core). `BuildImage`
is now a thin `chdir(CacheDir) + buildCmd.Run()`.

R10 (fully tested and validated): go test ./... green; task build:charly; `charly box build
@github.com/overthinkos/fedora/fedora` on the fresh rebuild — BuildImage executed live, cloned the remote
(HTTPS), and built the full fedora set (fedora / fedora-nonfree / fedora-builder :2026.182.0711) end-to-end,
exit 0, with R8 labels present (ai.opencharly.version). Env note: github git routed over HTTPS via the gh
token (`url.https://github.com/.insteadOf git@github.com:`) because SSH has no key this session — the same
HTTPS fallback used for pushes.

### 2026-07-01 — feat(charly): externalize the BUILD-ENGINE DISPATCH → candy/plugin-build over the F10 HostBuild seam (C14-core)

`charly box build` + `charly box generate` now route through a COMPILED-IN plugin (candy/plugin-build,
`build:box` + `build:generate`) instead of calling `NewGenerator` inline. The heavy engine — the
Generator, the OCITarget, the runtime Candy graph — STAYS host-side in-process, UNCHANGED (it is
I/O-bound with unexported state + a huge blast radius, RDD-confirmed as immovable); only a small wire
envelope crosses the seam. The group/substrate/candy echo pattern applied to build. (C14.1+C14.2
combined per RDD — a registered-but-unused host-builder would be dead code, R5. PrepareVenue is
untouched; that is C14.3.)

- **New `build` provider class** (`ClassBuild`, provider.go + providerClasses + the `#PluginCapability`
  CUE regex): a class whose Invoke calls back the F10 HostBuild seam. Serves `build:box` +
  `build:generate`. No new per-class bijection gate needed (only verb/kind/deploy/step have one).
- **New `OpBuild` selector** (`sdk.OpBuild = "build"`) + `provider.go` alias.
- **spec.BuildRequest / spec.BuildReply** (spec/deploy_wire.go, sibling to BuildEnv/EmitReply): the
  build-engine dispatch envelope. BuildRequest carries the CLI inputs NOT reconstructable from Dir
  (Boxes/Tag/Dir/IncludeDisabled/DevLocalPkg + the buildImages knobs Push/Platform/Cache/NoCache/
  Jobs/PodmanJobs); everything the engine reads (Config/ResolvedBox/Candy) is reconstructed HOST-SIDE
  from Dir inside the host-builder (exactly as pod_deploy_lifecycle re-runs NewGenerator(dir,…)).
  BuildReply{Written, Error} — a build FAILURE rides Error (the plugin echoes it; the RPC succeeds).
- **IN-PROC reverse channel** (plugin_inproc_reverse.go + sdk executor.go) — the minimal addition RDD
  showed was needed: a COMPILED-IN plugin has no go-plugin broker, so it cannot dial the gRPC HostBuild
  reverse channel. `inprocExecutorClient` implements `pb.ExecutorServiceClient` by delegating DIRECTLY
  to a host-side `executorReverseServer` (no socket); `sdk.NewInProcExecutor` + `sdk.ContextWithExecutor`
  + `sdk.ExecutorForInvoke(ctx, brokerID)` (ctx-first, broker-fallback) thread it to the plugin's Invoke
  transport-invisibly — so the plugin's Invoke code is byte-identical compiled-in vs out-of-process. It
  deliberately does NOT reuse `executorInvoker` (which stays grpcProvider-ONLY — it is the discriminator
  that routes external verbs to `ExternalPluginStep`; inprocProvider must not collide with it); the
  in-proc reverse channel is threaded by `dispatchBuild` inline. This is the general foundation the
  "every builtin routes through the reverse channel" direction needs.
- **image + containerfiles host-builders** (build.go, on the F10 `hostBuilders` map): `image` runs the
  FULL image build (NewGenerator + Generate + buildImages + merge + push + retention) host-side;
  `containerfiles` runs the generate path. The kinds are class-generic ACTION nouns — NOT the
  build:box/build:generate provider WORDS (the F11 uniform-API gate `TestNoSinglePluginAPISurface`
  forbids a provider word on that surface; `generate` would have collided, hence `containerfiles`).
- **HARD-CUTOVER (R5):** `BuildCmd.Run` + `GenerateCmd.Run` are now thin dispatchers
  (`dispatchBoxBuild` / `dispatchBoxGenerate`); the inline NewGenerator+Generate (+ buildImages/push/
  retention) bodies moved into the host-side `runBoxBuild` / `runBoxGenerate`. `buildImages` now returns
  the built image refs (BuildReply.Written). `ensureBuilderImageBuilt` calls `runBoxBuild` directly (the
  host engine recursing on a dependency, not the CLI re-entering). `git grep NewGenerator` shows ZERO
  direct calls in the two Run methods.
- **candy/plugin-build** (new module) — compiled-in (in `compiled_plugins:`, regen'd plugins_generated.go
  + go.work), dual-placement (cmd/serve for out-of-process parity). Its Invoke forwards the
  host-constructed BuildRequest (op.Params, verbatim) to `Executor.HostBuild(kind, …)` and ECHOES the
  BuildReply — build:box → HostBuild("image"), build:generate → HostBuild("containerfiles").
- **Verified:** `go build`/`vet`/`test ./...` green; candy/plugin-build builds standalone (GOWORK=off);
  TestGenReproducible + TestPluginsGenReproducible + TestNoSinglePluginAPISurface green; `box validate`
  clean; and a REAL image built end-to-end via the new path — `charly box build fedora` →
  ghcr.io/overthinkos/fedora:2026.182.0642 (237 MB), OCI labels present (ai.opencharly.version /
  .box / .platform.distro).
- **Follow-up (R2, non-blocking):** `remote_image.go` `RemoteImageContext.BuildImage` carries a
  redundant standalone NewGenerator+Generate before its `buildCmd.Run()` (which now routes through the
  plugin) — a pre-existing preflight, out of this cutover's scope; route it through dispatchBoxGenerate
  (or drop it) as its own next cutover.

### 2026-07-01 — feat(charly)!: externalize the `candy` box⊻layer factory kind → candy/plugin-candy-kind (C2-candy — the LAST structural kind)

The final structural-kind externalization after C2-group + C2-substrate: the `candy` box⊻layer
factory — the built-in `candyKind` — moves out of charly core into a COMPILED-IN plugin candy.
EVERY authoring kind is now plugin-served; the `#Node` disjunction has ZERO built-in arms and
`spec.KindWords` is EMPTY.

- **RDD dissolved BOTH historical blockers (verdicts with file:line):**
  - **BLOCKER 1 (bootstrap ORDERING) — NOT a wall for compiled-in.** `candyIsImage`
    (`charly/node_candy.go:23`) + `buildCandy` (`node_candy.go:39`) are core pure functions called
    DIRECTLY by the discovered-candy pre-check (`unified.go:1944`) + `scanCandy` (`layers.go:478`) —
    they STAY core. Only the INLINE candy path (`normalizeNodeInto` → the provider) reroutes through
    the plugin, and that runs at LoadUnified time (a command runtime), AFTER every `init()`. A
    COMPILED-IN plugin-candy-kind registers at `init()` (plugins_generated.go), so it is ALWAYS present
    when any candy decodes. The old "bootstrap cycle" the `candyKind` comment described exists ONLY
    for an EXTERNAL (fetch+build) candy plugin — compiled-in has no fetch → no cycle.
  - **BLOCKER 2 (#Node arm / #NodeDoc gate) — resolved with the substrate sub-foundation.** The
    `#NodeDoc` gate is base-schema-only (does NOT splice plugin schemas), and a plugin cannot serve a
    self-contained schema for the rich `#Candy`/`#Box` value. So — exactly as substrate did — the KEPT
    `#CandyValue` core def is the value gate, validated HOST-SIDE (`validateKindValueCUE`), and the
    decode routes through the plugin. With candy gone, `#Node` becomes an OPEN struct (`{...}`) — the
    structural gate only; per-kind value closedness is host-side. RDD proved a canonical `spec.Box` /
    `spec.Candy` round-trips through JSON byte-faithfully (0 `json:"-"` in the generated spec types).
- **candy/plugin-candy-kind** (new) — a COMPILED-IN kind plugin serving `kind:candy`
  (`Structural:FALSE` — candy nests NO deploy members; a resource sub-entity under candy stays a hard
  "sub-entity child" load error, so it must NOT set the F5 member-nesting flag; routed to
  `foldCandyKind` by an explicit `gn.disc=="candy"` host branch). Its `OpLoad` ECHOES the
  host-pre-decoded `spec.Box` (candy-image) / `spec.Candy` (candy-layer). Named `plugin-candy-kind`
  because `candy/plugin-candy` already exists (the DISTINCT `command:candy` CLI authoring plugin).
- **The seam.** `spec.StandaloneLoad` (charly/spec/deploy_wire.go) gained `Box`/`Candy` fields +
  `candy-image`/`candy-layer` shapes. `runPluginKind` gained a `gn.disc=="candy"` branch → new
  `foldCandyKind` (`provider_kind_invoke.go`): validate value against `#CandyValue`
  (`validateKindValueCUE`, generalized from the substrate `validateStandaloneKindValueCUE` +
  `kindValueDef` now carries `candy → #CandyValue`), run the core `candyIsImage` + `buildCandy` (the
  SINGLE decode source, unchanged), thread via `op.Env`, fold the echo into `uf.Box`/`uf.Candy`.
- **DELETED (R5):** `charly/plugin_candy.go` (the built-in `candyKind` + its `registerDedicatedBuiltin`).
  `candyIsImage`/`buildCandy` stay in `node_candy.go`. `git grep candyKind` returns only explanatory
  "former …" comments + the coincidental `candyKindFirst` legacy-shape test fixture (not the type).
- **CUE `#Node` surgery (schema/node.cue):** removed `#CandyArm` (the LAST arm) + the now-unused
  `_reservedNode`; `#Node: {...}` (open). KEPT `#CandyValue`/`#Image`/`#Candy`/`#Box`. `nodeDiscriminators`
  (schemagen) already handled a zero-arm `#Node` → `KindWords` is now `[]string{}`; `ResourceKinds`
  unchanged. Regenerated `spec/vocab_gen.go` + `spec/cue_types_gen.go` + `plugins_generated.go` + `go.work`.
- **Downstream fixes (empty KindWords + open #Node):** `rootShapeKeySet` (`reserved_registry.go`) unions
  `candy` via `legacyKindAliases` so a legacy top-level `candy:` doc still gets the `charly migrate` hint
  (the RejectsLegacyShapes "kind-keyed candy" case); `kit.NodeShapedValue`
  (`charly/plugin/kit/migrate_helpers.go`) adds `candy` so `charly migrate` stays idempotent + classifyDoc
  recognizes a node-form candy value; `validateCandyManifestCUE` (`cue_schema.go`) restores the CONCRETE
  candy-value gate (version+description required) against `#CandyValue` (the open `#Node` no longer does
  it); `TestCueKinds_Corpus` now validates the corpus against `kindValueDef` (the host-side gate) not the
  emptied `kindWordSet`; `nodeFormRejected` (test helper) runs the full node decode; `uniform_api_gate_test`
  probes the word universe instead of `KindWords[0]` (empty). No authored WIRE-key changed (candy authored
  identically) → NO schema-version bump / no migration step.
- **R10:** box validate CLEAN across ALL 6 repos (root + box/{fedora,cachyos,arch,debian,ubuntu}) — candy
  is THE core entity, so this is the ecosystem-wide decode proof (every box IMAGE + every composed LAYER
  folds through plugin-candy-kind). `TestCandyKind_BothShapesByteEquivalent` locks BOTH shapes (image →
  uf.Box == decodeNodeValue; layer → uf.Candy == buildCandy) against the direct core decode. The candy
  decode is exercised LIVE by check-pod (builds an image via candy) + check-substrate/check-group (compose
  candy layers via a local deploy). `go build/vet/test ./...` all green (incl. TestGenReproducible,
  TestPluginsGenReproducible, TestReservedWordRegistry_*); plugin-candy-kind `go build ./...` green; gofmt clean.

### 2026-07-01 — feat(charly)!: externalize the pod/vm/k8s/local/android structural kinds → candy/plugin-substrate + the F5 TEMPLATE-map fold arm (C2-substrate)

The next core-externalization cutover after C2-group: the 5 SUBSTRATE structural kinds
(pod/vm/k8s/local/android) — the shared built-in `standaloneKind` — move out of charly core into ONE
COMPILED-IN structural plugin candy, and the F5 authored-member input-threading foundation gains a
TEMPLATE-map fold arm (it previously folded a structural reply ONLY into `uf.Bundle`; now a substrate
node in standalone-TEMPLATE shape folds into the typed template map `uf.Pod`/`uf.VM`/`uf.K8s`/`uf.Local`/
`uf.Android` — the PRIMARY VM authoring form).

- **THE FOUNDATION (the F5 TEMPLATE fold arm).** `spec.StructuralKindLoadEnv` gains a `Standalone`
  channel (`spec.StandaloneLoad{Shape, Deploy, Template}`, `charly/spec/deploy_wire.go`). For a
  substrate kind, the host (`runPluginKind` → new `foldSubstrateKind`, `charly/provider_kind_invoke.go`)
  detects the shape, pre-decodes the CANONICAL node via the core loader (`buildBundleNode` for the
  deploy shape, `decodeStandaloneTemplateJSON`/`decodeNodeValue` for the template shape — the SINGLE
  decode source of truth, R3), threads it to the plugin's `OpLoad` via `op.Env`, and folds the plugin's
  ECHO into `uf.Bundle` (deploy) or the typed template map by kind (template — the new arm).
- **DESIGN (i), RDD-chosen for soundness.** A substrate value is RICH + core-referencing
  (`#Vm`/`#Deploy`/`#LibvirtDomain`/… with host-canonicalized shorthand like `tunnel:`/`port:` via
  `NormalizeEntityNode`), so — unlike group's small scalar `#GroupInput` value — it CANNOT be re-decoded
  soundly from `op.Params` by a plugin NOR validated by a self-contained plugin schema (`BuildCapabilities`
  compiles the schema standalone; a base-referencing def fails). So the HOST pre-decodes + validates the
  value against the KEPT `#<Kind>Value` core defs (`validateStandaloneKindValueCUE`, replacing the
  removed `#Node` arm's closedness), and the plugin is a PURE ECHO. RDD proved a canonical
  `spec.Deploy`/`spec.Vm`/`spec.Pod` round-trips through JSON byte-faithfully, so thread-echo-fold is
  byte-equivalent to the former in-proc `standaloneKind` decode. `TestSubstrateKind_BothShapesByteEquivalent`
  locks BOTH shapes against the direct core decode (`buildBundleNode` / `decodeNodeValue`).
- **candy/plugin-substrate** (new) — a COMPILED-IN structural kind plugin serving all 5 words
  (`kind:pod/vm/k8s/local/android`, Describe `Structural:true`, `InputDef:""` → host-validated). Its
  `OpLoad` ECHOES the host-pre-decoded `op.Env.Standalone` (deploy → `spec.Deploy`; template → the typed
  JSON verbatim). Root pkg + `cmd/serve` shim + a self-contained `schema/substrate.cue` (`#SubstrateKindLoad`
  — non-empty gate only; the value is host-validated, not schema-validated).
- **PLACEMENT — COMPILED-IN** (added to the embedded `charly/charly.yml` `compiled_plugins:`), NOT
  external: the 5 substrates are core deploy primitives every box/submodule authoring
  `pod:`/`vm:`/`k8s:`/`local:`/`android:` must ALWAYS resolve (the root check/vm/local/k8s entities,
  box/fedora, box/cachyos, box/arch, box/debian, box/ubuntu), exactly like group + the tier-1 kinds.
- **DELETED (R5 hard cutover):** `charly/plugin_substrate.go` (the shared built-in `standaloneKind` +
  its 5 `registerDedicatedBuiltin` registrations + `isDeployShape`, moved to `node_normalize.go`),
  `charly/node_normalize.go`'s `buildStandaloneResource` + `decodePtrInto` (superseded by
  `decodeStandaloneTemplateJSON`/`foldStandaloneTemplateReply`/`foldTemplateReply`). `git grep
  standaloneKind`/`buildStandaloneResource` returns ONLY explanatory "former …" comments (the
  established C2-group precedent — cf. `groupKind` in candy/plugin-group).
- **CUE `#Node` surgery (`charly/schema/node.cue`):** removed `#PodArm`/`#VmArm`/`#K8sArm`/`#LocalArm`/
  `#AndroidArm` from the `#Node` disjunction and pod/vm/k8s/local/android from `_reservedNode` — so
  `#Node: #CandyArm` (candy is the ONLY remaining core kind arm) and a substrate node passes `#NodeDoc`
  via `#CandyArm`'s open `{[!~_reservedNode]: _}` pattern. The `#PodValue`/`#VmValue`/… defs are KEPT
  (host-side value gate). The 5 substrates STAY in `#ResourceKind` (`resourceKindSet` still nests their
  members). `nodeDiscriminators` (`charly/internal/schemagen`) gained single-arm handling (a one-arm
  `#Node` is not an `OrOp`) so `KindWords = {candy}` (not empty). Regenerated `spec/vocab_gen.go`
  (KindWords loses the 5, ResourceKinds keeps them), `spec/cue_types_gen.go`, `plugins_generated.go` +
  `go.work` (pluginsgen — plugin-substrate compiled in).
- **Downstream bijection/detector fixes (all keyed off the split KindWords ⊄ ResourceKinds):**
  `checkDeployProviderBijection` now checks deploy-target words ⊆ `spec.ResourceKinds` (not KindWords —
  the substrate deploy words left KindWords); `rootShapeKeySet` (`charly/reserved_registry.go`, the
  legacy-shape DETECTOR) unions in `spec.ResourceKinds` so a legacy `pod:`/`vm:`/… collection map is
  still rejected with a `charly migrate` hint; `kit.NodeShapedValue` (`charly/plugin/kit/migrate_helpers.go`)
  unions in `spec.ResourceKinds` so `charly migrate` stays idempotent on an entity NAMED after a substrate
  kind. No authored WIRE-key changed (`pod:`/`vm:`/… author identically), so NO schema-version bump / no
  migration step.
- **R10 beds:** `check-substrate` (new, root charly.yml) — a `local:` DEPLOY (deploy shape → uf.Bundle)
  inheriting a BARE `local:` TEMPLATE `check-substrate-app` (template shape → uf.Local), BOTH decoded
  through the compiled-in plugin-substrate; applies the new `candy/check-substrate-layer` marker candy to
  the host and asserts `/etc/check-substrate-marker` (collision-free path). The rich `vm:` TEMPLATE fold
  (primary VM form) is proven byte-equivalent by `TestSubstrateKind_BothShapesByteEquivalent` and LIVE by
  box/debian's `check-debian-debootstrap-vm` (a `vm:` template + `vm:` deploy, now routed through
  plugin-substrate).
- **Verification:** `go build ./... && go vet ./... && go test ./...` all green (incl. `TestGenReproducible`,
  `TestPluginsGenReproducible`, `TestReservedWordRegistry_*`, the byte-equivalence proof); `candy/plugin-substrate`
  `go build ./...` green; `box validate` clean across root + box/{fedora,cachyos,arch,debian,ubuntu} (the
  blast-radius check — every real pod/vm/k8s/local/android user folds through plugin-substrate); gofmt clean.

### 2026-07-01 — feat(charly)!: externalize the `group` structural kind → candy/plugin-group (C2-group)

The clean consumer of the F5 authored-member input-threading foundation: the `group` deploy KIND (a
TARGETLESS deploy group — resource members alongside on the shared network, no own workload) moves out
of charly core into a COMPILED-IN structural plugin candy, and the builtin `groupKind` is deleted (R5).

- **candy/plugin-group** (new) — a compiled-in structural kind plugin (Describe `Structural:true`).
  Its `OpLoad` decodes the group's scalar config (disposable/lifecycle/description/iterate/…) from
  `op.Params` into a `spec.Deploy`, ATTACHES the AUTHORED members the host pre-decoded + threaded via
  `op.Env` (`spec.StructuralKindLoadEnv`, F5), forces `Target=""` (targetless → members are PEERS), and
  returns the complete `spec.Deploy` the host folds into `uf.Bundle` — BYTE-EQUIVALENT to the former
  builtin `groupKind` (`TestExternalStructKind_StructuralDecode` proves the invariant against the
  builtin path; the `check-group` bed proves it live). Root pkg + `cmd/serve` shim; decodes into
  `spec.Deploy` (no generated `params/` needed). Self-contained `schema/group.cue` `#GroupInput` — a
  PURPOSE-BUILT targetless-group value surface (identity/lifecycle scalars closed for typo-protection;
  `iterate`/`ephemeral`/`preemptible`/`plan` permissive, validated on the folded Bundle by
  validateIterateBed / validate_ephemeral / validateOps — no `#Deploy`/`#Iterate` clone).
- **PLACEMENT — COMPILED-IN** (added to the embedded `charly/charly.yml` `compiled_plugins:`), NOT
  external: `group` is a core deploy primitive every box/submodule authoring `group:` must ALWAYS
  resolve (check-k8s-deploy, box/fedora, box/cachyos, the charly-cli AI-benchmark group), exactly like
  the tier-1 kinds (distro/target/…), which are all compiled-in. RDD confirmed the checkKindProvider
  bijection demands a typed KindProvider per KindWord, so a plugin kind must LEAVE `spec.KindWords`.
- **DELETED (R5 hard cutover):** `charly/plugin_group.go` (the builtin `groupKind` + its
  `registerDedicatedBuiltin`) and `charly/cue_kind_group.go` (`registerCueKind("group","#Deploy")`) —
  matching how every prior externalized kind (distro/target/agent/package-group) was deleted, not
  signposted. `git grep groupKind` is clean.
- **CUE `#Node` surgery (`charly/schema/node.cue`):** removed `#GroupArm` from the `#Node` disjunction,
  `#GroupValue`, and `group` from `_reservedNode` — so a `group:` node passes `#NodeDoc` via the arms'
  open `{[!~_reservedNode]: _}` pattern (a non-core discriminator) and its VALUE is validated by
  plugin-group's served `#GroupInput` (runPluginKind). `group` STAYS in `#ResourceKind` (kept the enum
  entry) so `resourceKindSet` still nests its members — `group` is now the FIRST resource kind that is
  NOT a KindWord (`ResourceKinds ⊄ KindWords`, a new-but-legal split; the two enums are independent).
  `uf.Bundle` + all consumers UNCHANGED. Regenerated `spec/vocab_gen.go` (KindWords loses `group`,
  ResourceKinds keeps it), `plugins_generated.go` + `go.work` (pluginsgen — plugin-group compiled in).
- **R5 STALE-DOC SWEEP** (the input path now EXISTS, so the claims are legitimately updatable): the
  refuted "the JSON Invoke envelope cannot thread the genericNode member tree" claim is gone from core
  (`plugin_group.go` deleted); `charly/plugin_substrate.go` rewritten — the DEPLOY-shape half is now
  F5-ready (group externalized through it), the substrate TEMPLATE half (uf.Pod/uf.VM/…) needs the F5
  EXTENSION (a follow-up); `charly/plugin_candy.go` rewritten — candy stays core for the ENDURING
  bootstrap-loader reason (candyIsImage/buildCandy run before plugins connect), NOT the refuted envelope
  claim. `git grep` of the refuted claim returns only the FALSE-stating comment.
- **check-group bed** (root `charly.yml`) + `candy/check-group-layer` + `check-group-app` template: the
  R10 runtime witness — a REAL targetless `group:` deploy group whose reconstructed `local:` member
  deploys on the host and whose hoisted marker check asserts `/etc/check-group-marker` (a collision-free
  path, so `/verify-beds` can fan it out concurrently). Proves a real `group:` decode routes through
  plugin-group → the F5 foundation → uf.Bundle and deploys end-to-end.

RCA note (R1): during `box validate` the nested connect-pass load failed on the charly-cli group's
`iterate:` (a folded data child) — `#GroupInput` initially omitted `iterate`; the swallowed nested error
surfaced downstream as a spurious examplestructkind gate error. Fixed by adding permissive `iterate?`
to `#GroupInput` (validated on the folded Bundle by validateIterateBed).

Verify (this change): `go build ./... && go vet ./... && go test ./...` all green (incl.
`TestExternalStructKind_StructuralDecode` byte-equivalence, node_bundle_test group cases via the
compiled-in provider, the corpus + node-doc gates, `TestPluginsGenReproducible`, `TestGenReproducible`);
`candy/plugin-group` builds standalone (`GOWORK=off go build`); `charly box validate` clean on the FULL
repo (compiled-in plugin-group folds every real `group:` user — check-k8s-deploy / charly-cli /
box-fedora); `gofmt -l` clean. R10 runtime gate: the `check-group` disposable bed
(`charly check run check-group`) — run at landing.

### 2026-07-01 — feat(charly): F5 authored-member INPUT-threading — structural kind plugins reconstruct authored member trees

The ENABLER (foundation) for externalizing the structural-kind decoders (`group` first, then the
substrate templates). RDD proved the pre-existing F5 channel only threaded a plugin's OUTPUT member
tree (`OpLoad` reply → `uf.Bundle`); the AUTHORED member children never reached the plugin —
`assembleEntityBody` (what `runPluginKind` feeds the plugin) SKIPS `entity`-class children, so an
external structural kind received only its scalar body (`{disposable:true}`) and could not reconstruct
authored members. The reference example SIDESTEPPED this by SYNTHESIZING a member from `marker`; a real
`group` would silently lose every member. This cutover threads the authored member tree IN.

- **Design (i) — host pre-decodes, plugin attaches** (chosen over moving the decoder to an importable
  seam): the member recursion is a GENERIC concern and STAYS core. `runPluginKind` pre-decodes a
  structural kind's authored resource-member children via the SAME core `buildBundleNode` recursion the
  builtin path uses, and threads the decoded subtree to the plugin's `OpLoad` via `op.Env`. The member
  tree CANNOT ride `op.Params` — that is unified against the plugin's CLOSED `#<Kind>Input` def, which
  the members would violate. The plugin decodes only its kind-specific scalar body from `op.Params` and
  ATTACHES the host-threaded members to its `spec.Deploy` reply — so the host folds a COMPLETE Bundle
  (with the AUTHORED members) into `uf.Bundle`, BYTE-EQUIVALENT to a builtin `group`. Cross-member
  `${HOST:…}` refs survive as literal strings resolved later by tree position, so host-side pre-decode
  is structure-preserving.
- `charly/node_bundle.go`: extracted `buildResourceMemberChildren` — the SINGLE source of truth for
  authored member-tree decode (R3). `buildBundleNode` (builtin path, partitions Members/Children by
  Target) AND `runPluginKind` (external structural path) both call it — the builtin decode is unchanged.
- `charly/provider_kind_invoke.go`: `runPluginKind` threads the pre-decoded members to `OpLoad` via
  `op.Env` for a structural kind; a FLAT (non-structural) kind carrying member children is now a HARD
  error (never a silent drop).
- `charly/spec/deploy_wire.go`: new `StructuralKindLoadEnv{Members}` wire type (SDK-importable — the
  plugin decodes it from `op.Env`).
- `charly/node_parse.go` + `charly/plugin_prescan.go`: the parser's sub-entity-child gate now admits
  members under a recognized external STRUCTURAL kind (`recognizedStructuralKind`, with pre-pass
  leniency for a declared-but-not-yet-connected kind via `isDeclaredExternalKind`); core non-resource
  kinds (candy/distro/…) stay guarded, and a flat plugin kind's members are caught in `runPluginKind`.
- `candy/plugin-example-structkind`: the reference OUT-OF-TREE structural plugin now decodes the
  deploy-config scalars (disposable/lifecycle/description) from `op.Params` and ATTACHES the
  host-threaded AUTHORED members (was: synthesized a single member from `marker` — never exercised
  authored-member reconstruction). Schema widened to the closed `{marker?,disposable?,lifecycle?,
  description?}`; version 2026.182.0440.
- `candy/check-structkind-layer/charly.yml` + root `charly.yml` `check-structkind` bed +
  `check-structkind-app` template: the disposable RUNTIME witness — an external structural-kind bed whose
  reconstructed `local:` member deploys on the host and whose hoisted marker check runs
  (`/etc/check-structkind-marker`, a collision-free path so `/verify-beds` can fan it out concurrently).
- Tests: `TestExternalStructKind_StructuralDecode` rewritten to PROVE byte-equivalence — the SAME
  authored member tree (two PEER pods, a NESTED pod-in-pod, a cross-member `${HOST:cache}` check) under
  `examplestructkind:` (real OOP plugin) vs `group:` (builtin) folds to an IDENTICAL `uf.Bundle` entry.
  `nodeHasPluginKindDisc` (corpus test) now recognizes a declared external structural kind (uses
  `recognizedKind`), so an external-structural-kind bed in the root `charly.yml` is exempted from the
  core `#NodeDoc` grammar corpus exactly as an external deploy substrate already is.
- **Scope**: the ENABLER ONLY. `group` and the substrate templates externalize ON TOP (next cutovers).
  The `charly/plugin_candy.go` + `charly/plugin_substrate.go` "deferred to the ExecutorService enabler
  (the JSON Invoke envelope cannot thread the genericNode member tree)" comments become legitimately
  updatable AFTER this lands (the input path now EXISTS) — swept in the `group` cutover, not here.

Verify (this change): `go build ./... && go vet ./... && go test ./...` all green (incl.
`TestExternalStructKind_StructuralDecode` building + running the REAL OOP plugin through the loader,
the corpus + node-doc gates, `TestPluginsGenReproducible`, `TestGenReproducible`); the example plugin
builds standalone (`GOWORK=off go build`); `charly box validate` clean on the full repo (connects the
OOP structural plugin for the `check-structkind` bed); `gofmt -l` clean. R10 runtime gate: the
`check-structkind` disposable bed (`charly check run check-structkind`) — run at landing.

### 2026-07-01 — feat(charly): proactive `user_allow_other` handling for encrypted volumes (C16a follow-up)

The C16a-surfaced follow-up: an encrypted-volume mount used to fail mid-plan with only the raw
`fusermount3: option allow_other only allowed if 'user_allow_other' is set` error. Now charly handles
the `-allow_other` host prerequisite proactively.

- `charly/enc.go`: `fuseAllowOtherEnabled()` reads `/etc/fuse.conf` for an active `user_allow_other`
  line (path via the `fuseConfPath` var, test-swappable); `encExecViaPlugin` PREFLIGHTS it for the
  mount/ensure methods and fails fast with the exact fix (`echo user_allow_other | sudo tee -a
  /etc/fuse.conf`) BEFORE any volume mounts partway.
- `charly/doctor.go`: `charly doctor`'s Encrypted Storage group now includes a `user_allow_other`
  check (`checkFuseAllowOther` — OK when set, WARNING + fix hint when missing).
- Tests: `charly/enc_fuse_test.go` (the helper across active/commented/absent/missing-file; the
  mount-method preflight error; the doctor OK/WARNING verdicts).

R10 (fully tested and validated): `go test ./...` green incl. the new tests; `task build:charly`; live
`charly doctor` shows `[+] user_allow_other -- /etc/fuse.conf`; `check-enc-pod` disposable bed PASS on
the fresh rebuild (the preflight-pass path + the real gocryptfs mount, unchanged).

### 2026-07-01 — feat(charly): externalize the gocryptfs mount mechanics → candy/plugin-enc (C16a)

The security-sensitive gocryptfs SHELLING (systemd-run `--scope --user gocryptfs -allow_other` + extpass +
init/mount/unmount/passwd loops) moves out of `charly/enc.go` into a COMPILED-IN candy `candy/plugin-enc`
(`verb:enc`/`OpExecute`), behind a thin in-core shim — the egress/k8sgen/migrate host-prelift pattern.

- **RDD-refined seam** (the key finding): the deploy-model + probes STAY core — `ResolvedBindMount` +
  `ResolveVolumeBacking` (deploy.go), `loadEncryptedVolume`, and the pure path/state helpers
  (`encryptedPlainDir`/`resolveEncVolumeDir`/`isEncryptedMounted`/`isEncryptedInitialized`/
  `cipherPopulatedPlainEmpty`) are consumed SYNCHRONOUSLY by mandatorily-core deploy code
  (`ResolveVolumeBacking`→`encryptedPlainDir`; `verifyBindMounts`→`isEncryptedMounted`), so only the
  gocryptfs *shelling* moved — the plugin never re-probes state or re-derives a charly path.
- `charly/enc.go`: `encMount`/`encUnmount`/`encPasswd`/`ensureEncryptedMounts` become thin shims;
  `encPlanFor` host-prelifts the resolved per-volume `[]spec.EncVolumePlan` (cipher/plain dirs + scope-unit +
  host-probed initialized/mounted flags) + the passphrase (resolved core-side via the credential store — the
  passphrase never leaves the in-proc compiled-in Invoke), and `encExecViaPlugin` Invokes `verb:enc`.
  `encStatus` stays core (pure probe+print). Credential resolution (`resolveEncPassphrase*`/keyring-await)
  stays core.
- New `charly/spec/enc_wire.go` (`EncExecInput`/`EncVolumePlan`/`EncExecReply`); `candy/plugin-enc`
  (compiled-in, ADE plan+check); `compiled_plugins += plugin-enc`; pluginsgen regen (`plugins_generated.go`
  + `go.work`); R5 sweep (go + enc skills). The 8 callers (deploy/config/quadlet/data/volumes) are unchanged.
- Preserved a pre-existing latent divergence faithfully (`ensureEncryptedMounts` scope-unit uses the bare box
  name vs `deployStorageDir` elsewhere — identical for empty-instance; a noted follow-up).

R10 (fully tested and validated): `go test ./...` green (enc unit tests kept in core); `task build:charly`
(plugin-enc compiled in); `TestPluginsGenReproducible` green; the `check-enc-pod` disposable fedora bed PASS
(steps=10) on the fresh rebuild — a REAL gocryptfs `-allow_other` mount via the externalized plugin, with the
container reading/writing the FUSE plain dir. **Host prereq:** the mount requires `user_allow_other` in
`/etc/fuse.conf` (a documented gocryptfs `-allow_other` requirement for rootless-podman keep-id, pre-existing —
not new to this cutover); it was enabled on the test host to run the bed. FOLLOW-UP (immediate-next): the enc
mount should preflight-check + hint on missing `user_allow_other` (today it surfaces the raw `fusermount3`
error), and `charly doctor`'s Encrypted Storage group should check it.

### 2026-07-01 — feat(charly)!: externalize `charly clean` / `settings` / `candy` → candy/plugin-{clean,settings,candy} (rest of C15)

The rest of cutover C15: three of the remaining WELDED machinery commands become out-of-tree
`command:{clean,settings,candy}` plugins, following the `candy/plugin-doctor` precedent exactly.
Each command's Run handler STAYS core (re-homed onto a hidden `charly __<word>`); a thin
`command:<word>` plugin raw-forwards `charly <word> <args>` → `syscall.Exec charly __<word> <args>` via
`cliMain`→`execCharly`. Works for both leaf commands (clean) and command trees (settings, candy) —
kong `passthrough` sends every flag/subcommand straight through, so one forwarder covers a leaf AND a tree.

**`charly version` was EXCLUDED from C15 — externalizing it breaks `pkgver()` (the build stamps the
package version via `bin/charly version`); version stays a core command (RDD-vindicated).** `pkg/arch`'s
`pkgver()` deliberately runs `${startdir}/../../bin/charly version` (to avoid a submodule-git mismatch);
a project-less, plugin-less dev `bin/charly` cannot resolve an externalized `version` → empty pkgver →
`task build:charly` aborts (an unfixable chicken-and-egg). `charly version` is also R9/freshness-critical.
So it stays a core command; `candy/plugin-version/` was NOT created.

- `charly/main.go`: delete the three public `Clean`/`Candy`/`Settings` fields (else the plugins' prescan
  panics on a duplicate command); add the hidden `CleanInternal`/`SettingsInternal`/`CandyInternal`
  (`__clean`/`__settings`/`__candy`) grouped with `__vm`/`__doctor`. The public `Version VersionCmd` field
  STAYS. Run handlers (clean.go, SettingsCmd in main.go, CandyCmd in scaffold_cmds.go) UNCHANGED.
  `charly new candy` (NewCandyCmd) is NOT touched — different command.
- R5: `charly/provider_command.go` + `charly/main.go` machinery-list comments drop clean/settings/candy
  (version stays listed as hardcoded machinery). `cli_model_cmd_test.go` keeps `version` PRESENT;
  `provider_command_test.go` adds absent-checks for clean/settings/candy (not version).
- `candy/plugin-{clean,settings,candy}/` — each a standalone Go module (command:<word>,
  sdk.Main→cliMain raw-forward, inert gRPC half, ADE build-context check). pkg/arch bakes all three at
  /usr/lib/charly/plugins. plugins skill sweep bumped here.
- R10 bed `check-commands-local` (+ `check-commands-app` template) in the root charly.yml runs
  `charly settings list` / `candy --help` / `clean --dry-run` host-side, each exit 0.

The `!` marks removal of the three public core-command surfaces (the user-facing commands are unchanged;
they now dispatch through the plugins).

### 2026-07-01 — feat(charly)!: externalize `charly doctor` → candy/plugin-doctor (C15) + fix a re-entrant-Once readiness deadlock

The fifth welded-command externalization (after tmux/preempt/feature/vm): `charly doctor` becomes an
out-of-tree `command:doctor` plugin. `DoctorCmd`/`DoctorCmd.Run` STAYS core (it drives package-main
host-detection symbols — `credentialHealth`, `DetectGPU`/`DetectAMDGPU`/`GPURunArgs`/`DetectHostDevices`,
`DetectVFIO`/`VfioGroupAccessible`/`MemlockLimitBytes` — an out-of-process plugin cannot reach) and is
re-homed onto a hidden `charly __doctor`; the thin `candy/plugin-doctor` raw-forwards `charly doctor <args>` →
`syscall.Exec charly __doctor` (the vm precedent). Baked into `/usr/lib/charly/plugins` via pkg/arch.

- Core: `charly/main.go` deletes the public `Doctor DoctorCmd` field, adds `DoctorInternal DoctorCmd
  cmd:"" name:"__doctor" hidden`. `doctor.go` unchanged. R5: `charly/provider_command.go` +
  `charly/main.go` comments drop `doctor` from the "machinery stays hardcoded" list.
- New `candy/plugin-doctor/` (command:doctor, sdk.Main → cliMain raw-forward; inert gRPC half; ADE
  description + a deterministic check). pkg/arch bakes it (both loops). Skill swept (plugins repo).
- R10 bed `check-doctor-local` (+ `check-doctor-app` template) added to the root charly.yml.

**R1/R2 deadlock fix (surfaced by this cutover, fixed here — blocking):** running `charly doctor` in a
project dead-locked. RCA (goroutine dump): `loadedReadiness()` (`charly/readiness_config.go`) is a
`sync.Once` whose resolver calls `LoadUnified`, which connects the project's kind plugins; each
`LocalTransport.Connect` threads readiness env via `readinessPluginEnv` → `loadedReadiness` — a
SAME-goroutine re-entry into the Once that blocks forever on its own mutex (`sync.Once` is not
re-entrant). Latent until now (doctor is the first command-plugin whose hidden handler does an in-project
plugin-connect via `credentialHealth`). Fix: a re-entrancy guard (`readinessLoading atomic.Bool`) returns
the built-in defaults on a re-entrant/concurrent-during-first-load call — correct, since a plugin connected
to LOAD the config cannot depend on the not-yet-resolved readiness, and the built-in defaults are always
safe. Regression test `TestLoadedReadiness_ReentrancyGuard`.

R10 (fully tested and validated): `go test ./...` green incl. the new regression test; `task build:charly`
(fresh install, plugin-doctor baked); live `charly doctor` (project-less full report + in-project
post-fix full report, exit 0); the `check-doctor-local` disposable bed PASS (steps=4) on the fresh
rebuild. The `!` marks the removed public `Doctor` core-command surface (the user-facing `charly doctor`
is unchanged — it now dispatches through the plugin).

### 2026-07-01 — docs: C13/M15 complete; RETRACT C13b (OpBootstrap auto-migrate-at-load) as unsound

C13a (the 2026-07-01 M15 entry) externalized the migrate chain to `candy/plugin-migrate` (serving `OpRun`).
The M15 entry promised a follow-up "C13b" that would make plugin-migrate a **bootstrap-phase** plugin serving
`OpBootstrap`, so a stale config's root bytes would auto-migrate in-memory at load via the F9 `runBootstrapPhase`
(the rev.3 vision: "the kernel reads raw `charly.yml` bytes → the migrate plugin returns the upgraded config").
RDD on the live code refuted that design, so C13b is RETRACTED and C13/M15 is declared COMPLETE as landed.

Why C13b is architecturally unsound (ground truth from the live code):

- The migrate chain is **whole-project file-based + host-coupled**, not a raw-root-bytes transform: `runDocMigration`
  scans candidate files across the whole project dir and rewrites each on disk; `rewriteDocFile` writes specific
  files + `.bak` backups; `migrateHostOverlayDoc` mutates `~/.config/charly/charly.yml` host state.
- `MigrateContext` needs **host-prelifted inputs** (`ImageNames` from `LoadUnified(dir)`, `BundleVolumes` from
  `LoadBundleConfig`, …) — produced by a *completed* `LoadUnified`. But `runBootstrapPhase` fires *inside*
  `LoadUnified`, before the parse → a re-entry cycle.
- `runBootstrapPhase` transforms **only the root bytes**; a stale project's candy files / siblings stay stale and
  fail the `#NodeDoc` / per-entity gates regardless. A partial in-memory pass would also let the trailing
  `calver-schema` stamp write **HEAD onto an incompletely-migrated doc** — masking incompleteness.

Resolution (operator-approved): keep the documented, safe model — the load gate REJECTS a stale config with
`Run: charly migrate`, and `charly migrate` (+ remote-cache auto-migration) runs the full file-based chain
explicitly (with backups). The F9 bootstrap-phase machinery stays as foundational infra, proven by the no-op
`candy/plugin-example-bootstrap` + the F9 tests; it simply has no production consumer yet.

R5 claim-keyed sweep (this commit): corrected every `.go` comment + the M15 CHANGELOG note that asserted migrate
(or egress) is/will-be a bootstrap-phase auto-migrate plugin — `charly/bootstrap_phase.go`, `charly/unified.go`,
`charly/plugin_bootstrap_test.go`, `charly/plugin/sdk/phase.go`, `candy/plugin-example-bootstrap/plugin.go`, and
the 2026-06 + 2026-07 CHANGELOG forward-claims; the `/charly-internals:go` skill is swept in the plugins repo.

### 2026-07-01 — style: gofmt-sweep the tree to zero (R2 hygiene)

`gofmt -l charly/ candy/` flagged 34 files carrying accumulated formatting dirt — struct-field
alignment that drifted as fields were added without re-running gofmt over the whole block (e.g.
`candy/plugin-spice/provider.go`'s `Box`/`Mode` not re-aligned to the wider `Substrate` field), plus
trailing whitespace from prior cutover edits. Pure formatting, zero semantic change.

- `gofmt -w` over `charly/` + `candy/`: 34 files (5 `charly/` — `check_kit_adapter.go`,
  `install_plan.go`, `plugin/sdk/checkverb.go`, `plugin_bootstrap_test.go`, `provider_checkenv.go` —
  and 29 `candy/plugin-*/` files across the builder/example/deploy/check plugins).
- `gofmt -l charly/ candy/` now returns empty; `go build ./...` / `go vet ./...` / `go test ./...`
  green (formatting has no runtime surface to bed-exercise — the toolchain run IS the gate).

### 2026-07-01 — feat(charly): externalize the migrate chain into candy/plugin-migrate via host-prelift (TRACK 2 / M15)

The schema-migration chain (~49 migrators + the registry, ~14.3K LOC) moves out of charly core into a new
COMPILED-IN candy; `charly/migrate.go` becomes a thin in-core shim. Unlike egress/k8sgen (pure transforms), the
migrate chain is LOADER-COUPLED — 5 migrators call core loader functions (`LoadUnified`, `LoadBundleConfig`,
`embeddedDefaults`, `VerbCatalog`) and manipulate package-main-only loader types a candy can't import. Resolved
via the operator-approved HOST-PRELIFT pattern.

- New `candy/plugin-migrate` (compiled-in, `verb:migrate`, serving `OpRun`): the registry
  (`MigrationStep`/`migrationSteps`/`runMigrations`, which now also returns the changed-step list) + the ~44
  loader-independent migrators + their tests, moved verbatim.
- **Host-prelift** for the 5 loader-coupled migrators: the in-core shim runs the loader-coupled lookups
  host-side and passes pre-resolved inputs (image names, local templates, bundle-volume summary, the
  build-vocabulary key set / host-deploy path) into the plugin's `OpRun`; those migrators consume the
  pre-resolved inputs instead of calling the loader — behavior preserved (the ported migrate tests pass). The
  act-verb set + credential store are injected at startup (constants/host singletons), not per-run.
- R3 relocations to `charly/plugin/kit` (one copy, both repos import): `CalVer`/`ParseCalVer`/`MustCalVer`/
  `LatestSchemaVersion`, `MigrateContext`/`MigrateReply`, `PluralToSingularYAMLKeys`, `LedgerSchemaVersion`, the
  path consts + shared yaml/file helpers (`FileExists`/`FirstNonEmpty`/`MapValue`/`FirstYAMLVersionLine`/…); the
  step-keyword consts; core aliases them via `kit_aliases.go`. `RejectLegacyPluralKeys` relocated to a
  non-migrate core file (it stays in core — a load-time gate).
- In-core shims (mirror egress.go/k8s_generate.go): `charly/migrate.go` (`RunMigrations`/`RunProjectMigrations`
  resolve `verb:migrate` + prelift + `Invoke(OpRun)`); `migrate_cmd.go` + `refs.go` consume the shim unchanged.
- R5: the moved `migrate_*.go` chain deleted from core; the loader-coupled migrate tests relocated to core
  (`charly/migrate_*_core_test.go`, importing the candy); `compiled_plugins:` + plugin-migrate; the stale
  `/charly-build:migrate` registry table extended to HEAD (2026.174.1100) + the go/cutover-policy skill refs.

This is C13a (the logic externalization) and it COMPLETES C13/M15: the migrate chain is fully externalized to
`candy/plugin-migrate` (serving `OpRun`), invoked by `charly migrate` + remote-cache auto-migration; the load
gate keeps the explicit `Run: charly migrate` reject for a stale config. (A once-planned C13b — an `OpBootstrap`
root-doc auto-migrate-at-load — was found architecturally unsound and RETRACTED; see the 2026-07-01 retraction
entry above.)

R10 (fully tested and validated): `go test ./...` green incl. the ~28 ported migrate tests (the 5 rewritten
loader-coupled migrators' tests preserved) + `TestGenReproducible` + `TestPluginsGenReproducible`; the candy's
`go test ./...` green; `task build:charly`; a LIVE migrate smoke (`charly migrate` in a `version: 4` temp
project → in-core shim → compiled-in plugin → chain → file rewritten to `2026.174.1100`); the `check-pod`
disposable bed PASSES on the fresh rebuild (the kit relocations + plugin registry + load path no-regression).
