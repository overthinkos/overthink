box:
    # mcp-protocol-probe: full Jupyter image (deploys as `mcp`).
    name: mcp
    base: fedora
    candy:
        - agent-forwarding
        - jupyter
        - notebook-templates
        - dbus
    port:
        - 8888:8888
