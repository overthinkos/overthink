candy:
    version: 2026.157.1917
    name: github-runner
    description:
        feature: A GitHub Actions self-hosted runner that builds CI jobs rootless inside a disposable box.
        narrative: |
            Installs the pinned actions/runner release under ${HOME}/actions-runner
            (config.sh + run.sh, run as uid 1000) plus the CI toolchain — jq/git/go/cosign,
            qemu-user-static for aarch64 binfmt cross-builds, and the rootless nested
            container stack (podman/buildah/skopeo via container-nesting). The .NET runtime
            deps (icu/krb5/openssl/libunwind/lttng-ust) are declared explicitly because the
            runner's installdependencies.sh has no Arch branch — so config.sh actually runs.
        tag: [working]
    require:
        - supervisord
        # Rootless nested podman/buildah/skopeo + subuid/subgid layout +
        # newuidmap/newgidmap file-caps + containers/storage/policy configs all
        # come from container-nesting. This layer carries only the
        # runner-specific pieces (R3 — no duplication).
        - container-nesting
    distro:
        arch:
            package:
                # Runner toolchain (podman/buildah/skopeo/crun/fuse-overlayfs are
                # provided by container-nesting).
                - jq
                - git
                - go
                - cloud-guest-utils # growpart
                - qemu-user-static # aarch64 cross-arch CI (binfmt)
                - qemu-user-static-binfmt
                - cosign # repo package (replaces binary download)
                # .NET runtime deps for the actions-runner binary — the runner's
                # bundled installdependencies.sh has no Arch branch, so its native
                # deps are declared explicitly here. (libz is NOT listed: CachyOS
                # ships zlib-ng-compat, which Provides zlib/libz.so — an explicit
                # `zlib` is an unresolvable conflict with it.)
                - icu
                - krb5
                - openssl
                - libunwind
                - lttng-ust
                # charly-host runtime deps that complete the `charly` PKGBUILD `depends=`
                # set ON THE RUNNER, so the release-packages CI job builds the pac
                # artifact NATIVELY here: with every dep already installed,
                # `makepkg -sf` resolves them all and never shells out to
                # `sudo pacman` (the rootless runner has no passwordless sudo).
                # The rest of depends= is provided by the charly/virtualization layers.
                - slirp4netns # rootless podman networking fallback
                - libisoburn # xorriso — VM NoCloud seed-ISO builder
                - cdrtools # genisoimage/mkisofs seed-ISO fallback
                - swtpm # software TPM 2.0 for VM guests
    volume:
        - name: state
          path: ~/actions-runner
    env:
        RUNNER_WORK_DIR: "${HOME}/actions-runner/_work"
        RUNNER_GROUP: "Default"
    env_accept:
        - name: RUNNER_ORG
          description: "GitHub org/user to register the runner with (e.g. overthinkos). Pass via: charly config githubrunner -e RUNNER_ORG=<org>"
    secret_accept:
        - name: RUNNER_TOKEN
          description: "Short-lived GitHub Actions runner registration token (gh api -X POST /orgs/<org>/actions/runners/registration-token). Credential-store backed; never written to deploy.yml or the quadlet."
    var:
        RUNNER_VERSION: "2.334.0"
    hook:
        # Registration only runs when a token is supplied — so a disposable bed
        # (no token) deploys the image without registering, and a stale token is
        # a clean no-op rather than an error.
        post_enable: |
            [ -z "${RUNNER_TOKEN:-}" ] && { echo "RUNNER_TOKEN empty — skipping runner registration"; exit 0; }
            : "${RUNNER_NAME:=${CHARLY_CONTAINER_NAME:-runner}}"
            : "${RUNNER_LABELS:=opencharly}"
            "${HOME}/actions-runner/config.sh" --unattended \
              --url "https://github.com/${RUNNER_ORG}" \
              --token "${RUNNER_TOKEN}" \
              --name "${RUNNER_NAME}" \
              --labels "${RUNNER_LABELS}" \
              --replace --work _work
        pre_remove: |
            [ -z "${RUNNER_TOKEN:-}" ] && { echo "RUNNER_TOKEN empty — skipping deregistration"; exit 0; }
            "${HOME}/actions-runner/config.sh" remove \
              --token "${RUNNER_TOKEN}"
    task:
        # GitHub Actions runner — pinned, declarative download (content-addressed
        # cache, reproducible). The runner self-updates against GitHub at runtime,
        # so a pinned base version is correct. Installed under $HOME (uid 1000),
        # not root-owned /opt. The download runs as root because the shared
        # /tmp/downloads content-cache mount is root-owned; the extracted tree is
        # chowned back to uid 1000 below so the rootless runner can write
        # _work/.runner/.credentials at runtime.
        - mkdir: "${HOME}/actions-runner"
          run_as: "${USER}"
        - download: "https://github.com/actions/runner/releases/download/v${RUNNER_VERSION}/actions-runner-linux-x64-${RUNNER_VERSION}.tar.gz"
          extract: tar.gz
          to: "${HOME}/actions-runner"
          run_as: root
        - command: |
            rh=$(getent passwd 1000 | cut -d: -f6)
            chown -R 1000:1000 "${rh}/actions-runner"
          run_as: root
        # Registry mirror for ghcr.io (host pull-through cache at 127.0.0.1:5000;
        # falls back to ghcr.io directly if absent). Rootless podman prefers
        # ~/.config/containers over /etc/containers.
        - mkdir: "${HOME}/.config/containers/registries.conf.d"
          run_as: "${USER}"
        - write: "${HOME}/.config/containers/registries.conf.d/ghcr-mirror.conf"
          run_as: "${USER}"
          mode: "0644"
          content: |
            [[registry]]
            prefix = "ghcr.io"
            location = "ghcr.io"
            [[registry.mirror]]
            location = "127.0.0.1:5000"
            insecure = true
    service:
        - name: github-runner
          exec: "%(ENV_HOME)s/actions-runner/run.sh"
          restart: always
          working_directory: "%(ENV_HOME)s/actions-runner"
          enable: true
          scope: system
    scenario:
        - name: runner-binaries-installed-and-runnable
          step:
            - then: the GitHub Actions runner is extracted under the home directory with its config.sh present
              file: "${HOME}/actions-runner/config.sh"
              exists: true
            - then: the runner's run.sh service launcher is present
              file: "${HOME}/actions-runner/run.sh"
              exists: true
            - then: config.sh executes, proving the .NET native deps (icu/krb5/openssl/libunwind/lttng-ust) resolved on this distro
              command: "${HOME}/actions-runner/config.sh --version"
              exit_status: 0
        - name: ci-container-toolchain-works
          step:
            - then: skopeo (rootless image copy) reports its version
              command: skopeo --version
              exit_status: 0
            - then: buildah (rootless image build) reports its version
              command: buildah --version
              exit_status: 0
            - then: cosign (image signing) reports its version
              command: cosign version
              exit_status: 0
            - then: the go toolchain used by CI jobs reports its version
              command: go version
              exit_status: 0
        - name: cross-arch-binfmt-available
          step:
            - then: the aarch64 qemu interpreter is installed so jobs can cross-build for arm64 via binfmt
              file: /usr/bin/qemu-aarch64-static
              exists: true
        - name: runner-runs-rootless-with-nested-podman
          step:
            - then: the runner service process runs as the unprivileged uid 1000, never root
              command: id
              stdout:
                - contains: "uid=1000"
              context:
                - runtime
            - then: rootless nested podman works inside the box (no mount_too_revealing EPERM)
              command: podman info
              exit_status: 0
              context:
                - runtime
            - then: a CI job can actually pull and run a container image rootless
              command: "podman run --rm quay.io/libpod/alpine:latest true"
              exit_status: 0
              context:
                - runtime
        - name: gr-runner-config-runs
          step:
            # --- build scope: the composition actually works (run the binaries) ---
            # config.sh executes the .NET Runner binary → proves the icu/krb5/.NET
            # native deps resolved on cachyos (the key cachyos-port signal).
            - id: gr-runner-config-runs
              command: "${HOME}/actions-runner/config.sh --version"
              exit_status: 0
        - name: gr-runner-run-sh
          step:
            - id: gr-runner-run-sh
              file: "${HOME}/actions-runner/run.sh"
              exists: true
        - name: gr-cosign
          step:
            - id: gr-cosign
              command: cosign version
              exit_status: 0
        - name: gr-skopeo
          step:
            - id: gr-skopeo
              command: skopeo --version
              exit_status: 0
        - name: gr-buildah
          step:
            - id: gr-buildah
              command: buildah --version
              exit_status: 0
        - name: gr-go
          step:
            - id: gr-go
              command: go version
              exit_status: 0
        - name: gr-git
          step:
            - id: gr-git
              command: git --version
              exit_status: 0
        - name: gr-qemu-aarch64
          step:
            - id: gr-qemu-aarch64
              file: /usr/bin/qemu-aarch64-static
              exists: true
        - name: gr-go-pkg
          step:
            - id: gr-go-pkg
              package: golang
              package_map:
                arch: go
              installed: true
        - name: gr-icu-pkg
          step:
            - id: gr-icu-pkg
              package: icu
              installed: true
        - name: gr-pkgbuild-dep-slirp4netns
          step:
            # charly-host runtime deps completing the `charly` PKGBUILD depends= set, so the
            # release-packages CI job builds the pac artifact natively (makepkg needs
            # no sudo when every dep is present). FAIL without the package: additions.
            - id: gr-pkgbuild-dep-slirp4netns
              package: slirp4netns
              installed: true
        - name: gr-pkgbuild-dep-libisoburn
          step:
            - id: gr-pkgbuild-dep-libisoburn
              package: libisoburn
              installed: true
        - name: gr-pkgbuild-dep-cdrtools
          step:
            - id: gr-pkgbuild-dep-cdrtools
              package: cdrtools
              installed: true
        - name: gr-pkgbuild-dep-swtpm
          step:
            - id: gr-pkgbuild-dep-swtpm
              package: swtpm
              installed: true
        - name: gr-rootless-uid
          step:
            # --- deploy scope: rootless runtime state ---
            # Runs as uid 1000 (NOT root) — the whole point of the rootless cutover.
            - id: gr-rootless-uid
              command: id
              stdout:
                - contains: "uid=1000"
              context:
                - runtime
        - name: gr-nested-podman-info
          step:
            # Rootless nested podman works (no mount_too_revealing EPERM).
            - id: gr-nested-podman-info
              command: podman info
              exit_status: 0
              context:
                - runtime
        - name: gr-nested-podman-run
          step:
            - id: gr-nested-podman-run
              command: "podman run --rm quay.io/libpod/alpine:latest true"
              exit_status: 0
              context:
                - runtime
        - name: gr-ghcr-mirror
          step:
            - id: gr-ghcr-mirror
              file: "${HOME}/.config/containers/registries.conf.d/ghcr-mirror.conf"
              exists: true
              context:
                - runtime
        - name: gr-newuidmap-cap
          step:
            - id: gr-newuidmap-cap
              command: getcap /usr/bin/newuidmap
              stdout:
                - contains: cap_setuid
              context:
                - runtime
