// Self-contained input schema for the verb:examplestep capability — references no
// base def, so it compiles standalone (gengotypes + the SDK's serve-side compile).
#ExamplestepInput: {
	marker?: string
}
