layer:
  name: blogwatcher
  description: Blog/RSS feed monitor CLI
  requires:
    - golang
  env:
    GOPATH: "~/go"
  path_append:
    - "~/go/bin"
  tasks:
    - cmd: |
        go install github.com/Hyaxia/blogwatcher/cmd/blogwatcher@latest
        go clean -cache
      user: "${USER}"
