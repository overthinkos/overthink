depends:
  - nodejs

path_append:
  - "~/.local/bin"

tests:
  - id: claude-binary
    file: ${HOME}/.npm-global/bin/claude
    exists: true
  - id: claude-version
    command: ${HOME}/.npm-global/bin/claude --version
    exit_status: 0
