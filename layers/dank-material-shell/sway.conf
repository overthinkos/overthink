# Safety net: hide any unexpected quickshell xdg_shell popups (e.g. changelog)
# DMS bar uses layer-shell so this only catches stray FloatingWindow instances
for_window [app_id="org.quickshell"] move scratchpad
