# Docker CE post-install config (packages + repo installed via layer.yml)
version: '3'

tasks:
  install:
    cmds:
      - mkdir -p /etc/modules-load.d && echo "iptable_nat" >> /etc/modules-load.d/ip_tables.conf
