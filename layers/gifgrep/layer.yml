layer:
  name: gifgrep
  description: GIF search and download CLI
  requires:
    - golang
  env:
    GOPATH: "~/go"
  path_append:
    - "~/go/bin"
  tasks:
    - cmd: |
        go install github.com/steipete/gifgrep/cmd/gifgrep@latest
        go clean -cache
      user: "${USER}"
