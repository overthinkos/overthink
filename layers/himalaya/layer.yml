layer:
    version: 2026.144.1443
    name: himalaya
    description: Himalaya email CLI (IMAP/SMTP)
    require:
        - rust
    task:
        - cmd: cargo install himalaya --locked
          user: "${USER}"
