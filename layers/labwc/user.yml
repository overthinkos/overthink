version: '3'

tasks:
  install:
    cmds:
      - mkdir -p ~/.local/bin ~/.config/labwc
      - cp /ctx/labwc-wrapper ~/.local/bin/labwc-wrapper
      - chmod +x ~/.local/bin/labwc-wrapper
      - cp /ctx/rc.xml ~/.config/labwc/rc.xml
      - cp /ctx/autostart ~/.config/labwc/autostart
