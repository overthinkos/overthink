version: '3'

tasks:
  install:
    cmds:
      - |
        git clone --depth 1 https://github.com/NVIDIA/NemoClaw.git /tmp/nemoclaw
        mkdir -p ~/.nemoclaw/blueprints
        cp -r /tmp/nemoclaw/nemoclaw-blueprint/* ~/.nemoclaw/blueprints/
        rm -rf /tmp/nemoclaw
