version: '3'

tasks:
  install:
    cmds:
      - mkdir -p ~/.local/bin ~/.config/openbox
      - cp /ctx/openbox-wrapper ~/.local/bin/openbox-wrapper
      - chmod +x ~/.local/bin/openbox-wrapper
      - cp /ctx/rc.xml ~/.config/openbox/rc.xml
