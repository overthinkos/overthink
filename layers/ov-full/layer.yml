description: Full ov toolchain (CLI + VM + crypto + console + podman machine)

rpm:
  packages:
    - gvisor-tap-vsock
    - podman-machine

pac:
  packages: []

layers:
  - ov
  - virtualization
  - gocryptfs
  - socat
