layer:
  name: ov-full
  description: Full ov toolchain (CLI + VM + crypto + console + podman machine)
  distros:
    fedora:
      packages:
        - gvisor-tap-vsock
        - podman-machine
  layers:
    - ov
    - virtualization
    - gocryptfs
    - socat
