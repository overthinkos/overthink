layer:
  name: rust
  path_append:
    - "~/.cargo/bin"
  distros:
    archlinux:
      packages:
        - rust
    debian:
      packages:
        - rustc
        - cargo
    fedora:
      packages:
        - rust
        - cargo
    ubuntu:
      packages:
        - rustc
        - cargo
  eval:
    - id: rustc-binary
      file: /usr/bin/rustc
      exists: true
    - id: rustc-version
      command: /usr/bin/rustc --version
      exit_status: 0
    - id: cargo-binary
      file: /usr/bin/cargo
      exists: true
    - id: cargo-version
      command: /usr/bin/cargo --version
      exit_status: 0
