description: Socket relay tool for VM console access and port relays

rpm:
  packages:
    - socat
    - iproute

pac:
  packages:
    - socat
    - iproute2

tasks:
  - copy: relay-wrapper
    to: /usr/local/bin/relay-wrapper
    mode: "0755"
    user: root
