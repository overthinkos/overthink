description: Audio spectrogram and visualization CLI

depends:
  - golang

env:
  GOPATH: "~/go"

path_append:
  - "~/go/bin"

tasks:
  - cmd: |
      go install github.com/steipete/songsee/cmd/songsee@latest
      go clean -cache
    user: "${USER}"
