version: '3'

tasks:
  all:
    cmds:
      - install -m 755 /ctx/sshd-wrapper /usr/local/bin/sshd-wrapper
      - |
        echo 'user ALL=(ALL) NOPASSWD: ALL' > /etc/sudoers.d/ov-user
        chmod 440 /etc/sudoers.d/ov-user
