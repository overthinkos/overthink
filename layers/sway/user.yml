version: '3'

tasks:
  install:
    cmds:
      - mkdir -p ~/.local/bin ~/.config/sway
      - cp /ctx/sway-wrapper ~/.local/bin/sway-wrapper
      - chmod +x ~/.local/bin/sway-wrapper
      - cp /ctx/config ~/.config/sway/config
