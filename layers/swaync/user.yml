version: '3'

tasks:
  all:
    cmds:
      - mkdir -p ~/.local/bin ~/.config/swaync
      - cp /ctx/swaync-wrapper ~/.local/bin/swaync-wrapper
      - chmod +x ~/.local/bin/swaync-wrapper
      - cp /ctx/config.json ~/.config/swaync/config.json
      - cp /ctx/style.css ~/.config/swaync/style.css
