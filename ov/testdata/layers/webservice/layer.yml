ports:
  - 8080
  - 9090

port_relay:
  - 8080

route:
  host: webservice.localhost
  port: 8080

volumes:
  - name: data
    path: "~/.webservice"

aliases:
  - name: websvc
    command: websvc-server
