version: '3'

tasks:
  ov:
    desc: Build ov from source and install (portable; auto-detects distro)
    dir: "{{.ROOT_DIR}}"
    cmds:
      - mkdir -p bin layers/ov/bin
      - cd ov && go build -o ../bin/ov .
      - cp bin/ov layers/ov/bin/ov
      - task: install

  install:
    desc: Install ov for the current host (auto-detects distro; portable by default)
    dir: "{{.ROOT_DIR}}"
    cmds:
      - |
        set -e
        if [ -r /etc/os-release ]; then
          . /etc/os-release
        else
          ID=unknown
          ID_LIKE=
        fi
        case " $ID $ID_LIKE " in
          *" arch "*|*" manjaro "*|*" archarm "*|*" endeavouros "*)
            if command -v makepkg >/dev/null 2>&1; then
              # pkg/arch/PKGBUILD declares mandatory AUR deps
              # (cloudflared-bin, gvisor-tap-vsock). Pre-install them via
              # an AUR helper FIRST, then run makepkg directly inside
              # pkg/arch. NEVER use yay -B / yay -Bi against the local
              # checkout — that mode runs `git pull` against pkg/arch's
              # subrepo and can reset uncommitted edits in the working
              # tree (war-story 2026-05-05).
              AUR_DEPS="cloudflared-bin gvisor-tap-vsock"
              if command -v yay >/dev/null 2>&1; then
                yay -S --needed --noconfirm $AUR_DEPS
              elif command -v paru >/dev/null 2>&1; then
                paru -S --needed --noconfirm $AUR_DEPS
              else
                echo "WARNING: yay/paru not found — AUR deps must be"
                echo "         pre-installed manually for makepkg to succeed."
                echo "         Install an AUR helper, then re-run."
              fi
              cd pkg/arch
              mkdir -p src/overthink-git
              cp "{{.ROOT_DIR}}/LICENSE" src/overthink-git/LICENSE
              # Auto-remove legacy ov-git / ov-git-debug packages if
              # present. The PKGBUILD declares replaces=('ov-git'), but
              # pacman -U (which makepkg -i invokes for local-file
              # installs) does NOT honor `replaces=` — that field only
              # fires during sync-database upgrades (pacman -Syu).
              # Without this pre-step the install hits an interactive
              # conflict prompt that --noconfirm answers "no" to, and
              # the install fails with `unresolvable package conflicts`
              # (for the main package) or `conflicting files` (for the
              # debug sibling). Both must be removed together.
              LEGACY_PKGS=""
              for pkg in ov-git ov-git-debug; do
                if pacman -Q "$pkg" >/dev/null 2>&1; then
                  LEGACY_PKGS="$LEGACY_PKGS $pkg"
                fi
              done
              if [ -n "$LEGACY_PKGS" ]; then
                echo "Removing legacy package(s):$LEGACY_PKGS (renamed to overthink-git)..."
                sudo pacman -Rdd --noconfirm $LEGACY_PKGS
              fi
              makepkg -sefi --noconfirm
              echo "Installed system-wide via pacman (makepkg in pkg/arch)"
              # One-time cleanup: the package is already installed via pacman, so
              # makepkg's src/, pkg/, the built *.pkg.tar.zst, and *.log are pure
              # transient waste. Always removed immediately so pkg/arch doesn't
              # accumulate (war-story: 138 stale *.pkg.tar.zst = 1.3G). The
              # `ov clean` verb sweeps these too. See /ov-core:clean.
              rm -rf src pkg ./*.pkg.tar.zst ./*.log
              exit 0
            fi
            ;;
        esac
        # Non-Arch (Fedora, Bazzite, Ubuntu, Debian, ...) or Arch without makepkg:
        # portable install to $HOME/.local/bin/ov.
        install -D -m 0755 bin/ov "$HOME/.local/bin/ov"
        echo "Installed portable binary to $HOME/.local/bin/ov"
        case ":$PATH:" in
          *":$HOME/.local/bin:"*) ;;
          *)
            echo "WARNING: $HOME/.local/bin is not in your PATH."
            echo "         Add it to your shell profile, e.g.:"
            echo "         echo 'export PATH=\"\$HOME/.local/bin:\$PATH\"' >> ~/.bashrc"
            ;;
        esac

  install-arch:
    desc: Force Arch package install (pre-resolves AUR deps via yay/paru, then makepkg)
    dir: "{{.ROOT_DIR}}"
    cmds:
      - |
        set -e
        AUR_DEPS="cloudflared-bin gvisor-tap-vsock"
        if command -v yay >/dev/null 2>&1; then
          yay -S --needed --noconfirm $AUR_DEPS
        elif command -v paru >/dev/null 2>&1; then
          paru -S --needed --noconfirm $AUR_DEPS
        else
          echo "WARNING: yay/paru not found — AUR deps must be pre-installed."
        fi
        cd pkg/arch
        mkdir -p src/overthink-git
        cp "{{.ROOT_DIR}}/LICENSE" src/overthink-git/LICENSE
        makepkg -sefi --noconfirm

  install-portable:
    desc: Force portable install to $HOME/.local/bin/ov (overrides distro detection)
    dir: "{{.ROOT_DIR}}"
    cmds:
      - install -D -m 0755 bin/ov "$HOME/.local/bin/ov"
      - echo "Installed to $HOME/.local/bin/ov"
